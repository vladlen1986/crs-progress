# OS-grade overhaul — judgment calls

> Decisions made where `brain/design/CRS-design-system.md` is silent or conflicts with the run prompt. The design system doc is the judge (autonomy contract §1). Reviewed by Vlad once at the end of the run.

| # | What | Where | Call | Why |
|---|---|---|---|---|
| 1 | "Approved literals §2.11" cited by the run prompt does not exist in the doc | all sweeps | Treat as the approved-literal set: `--white` text on accent/status fills, the §2.6 module-status-tag hexes, §20.7's noted rgba badge fills *until* re-tokenized, and shadow/tint rgba values inside the §2.10 block itself. Everything else must be a token. | Nearest doc anchors: §2.5.1 utility colors, §2.6 tag tables, §20.7 app-drift note. |
| 2 | Run prompt says modal widths "420/440/520" | modals sweep | Doc §5 says width 380–460px typical — doc wins; existing modals normalize into that range (settings modal may exceed as a full workspace modal — logged as its own call if kept). | Autonomy contract: the design system is the judge, not the prompt's from-memory numbers. |
| 3 | Run prompt says input focus = "accent @55% border — no glow rings" | inputs sweep | Doc §2.2/§5/§14: focus = `--input-border`→accent + 3px `--accent-glow` ring. Doc wins. `--accent-border-active` (the 55% rgba) stays for *selected chips/selection borders* per §2.3. | §14 defines Focused = 3px glow ring explicitly. |
| 4 | No tokens exist for code-block surfaces, prose body in rendered docs, or translucent "veil" overlays | index.html app-specific block | Kept as page vars (`--code-bg/--code-fg/--prose/--veil/--veil-2/--head-bg`) with paired light values, marked approved literals. Fixed the broken self-referential `--code-fg`/`--prose` dark values (#D4D4D8 / #C9C9CE ≈ between text-primary/secondary). | §2 has no code/prose tokens; §20.6 table spec nearest but not applicable. |
| 5 | Header theme control | index.html header | Icon-only (moon/sun) segmented at 28px next to the settings gear, reusing the app's `.seg` (active = `--bg-elevated`+`--text-primary` per §6's theme-toggle row; §20.1 smallest size 28). Light active segment gains a 1px-3px shadow per §3.3 elevation. | §6 specs the theme toggle inside the user menu; this app has no user menu — header is the equivalent surface. Labels dropped for header density; aria-labels kept. |
| 6 | Galaxy-map constellation category colors (`--c-mem1..5`, `--c-skill`, `--c-routine`, `--c-app`) | map.html | Kept as page-level data-viz literals in both themes. | No canonical tokens for category/data-viz colors; §2.5 status colors are semantically wrong for node categories. |
| 7 | Map surfaces were an off-palette darker set (#0A0A0C/#141417/#1C1C21) | map.html | Replaced by canonical surfaces via tokens.css aliases — the map now uses `--bg-primary/--bg-secondary/--bg-tertiary` like every other screen. Visual change: map background lightens slightly to #181818. | Inventory verdict NON-COMPLIANT; §2.1 defines the only approved surface set. |
| 8 | Legacy alias token names (`--panel`, `--line2`, `--good`…) kept app-wide | all pages | Aliases stay as documented var() derivations in tokens.css; canonical names required for new code only. A ~700-occurrence mechanical rename was churn without compliance value — every alias resolves to the canonical token per-theme. | §2 requires token *references*, which aliases are; inventory OFF-TOKEN rows reclassified compliant-by-alias. |
| 9 | Accent-as-text carrier | app-wide | The app's accent text/icons ride `--accent2` → `--accent-soft` (#60A5FA dark / #2563EB light, ~4.5:1 AA on white) rather than `--accent-text` (#1D4ED8, 5.8:1). Kept: AA-passing and consistent. New code should prefer `--accent-text` for small text. | §2.3 defines both roles; §18 requires ≥AA — met. Phase-1 verifier finding, ratified. |
| 10 | tree.html `.mv` reorder buttons 28px wide but 16px tall | tree.html | Two stacked 28px buttons would double module-row height; hit-width met. | §20.1 min size compromise, logged. |
| 11 | Dashed-underline inline-edit fields get accent underline on focus, no 3px ring | tree/wishlist | A glow ring on a borderless field reads as a box. | §14 ring applied to boxed inputs only. |
| 12 | Drag-drop insertion indicator keeps a 2px accent box-shadow | tree.html | Functional indicator, not card elevation. | §2.7 not violated in spirit. |
| 13 | Native checkboxes styled via `accent-color: var(--accent)` | tree/settings | One-line token-driven fix; full §20.2 custom rebuild needs new markup — deferred. | §20.2 nearest-pattern. |
| 14 | Modal overlay scrim `rgba(0,0,0,.55)` (dark) / index uses themed `--overlay` | wishlist + index | No scrim token exists in §2 — index defines app-level `--overlay` pair; wishlist keeps the commented literal. Candidate for a canonical `--scrim` token. | §5. |
| 15 | Purple tint via `color-mix(in srgb, var(--purple) 13%, transparent)` | activity.html + dashboard | §2.5.1 defines tints only for success/warning/error; color-mix keeps purple theme-paired. Candidate `--purple-tint` token. | §2.5.1 nearest-pattern. |
| 16 | Text glyph buttons (↑/↓/✕, ⏸/▶) not swapped to Feather SVGs | queue/wishlist | Outside the scoped fix families; sizes/colors tokenized. Follow-up under §16. | §16. |
| 17 | System font stacks kept on standalone pages (not Inter) | 5 standalone pages | Deliberate scope hold — §4 Inter adoption is an app-wide typography pass, not a color sweep. | §4, deferred. |
| 18 | Galaxy glow/nebula/comet canvas art | map.html canvas | Sanctioned canvas-art exception — all sprites token/constellation-derived, dimmed to 55% alpha in light so decorations never wash out #FAFAFA. No DOM element carries a glow. | §2.7 governs UI surfaces; the map's premise is a galaxy. §3.3 readability preserved. |
| 19 | Dark-document preview backdrop stays #181818 in both themes | map.html .pv iframe.dark | Approved literal — it backs dark HTML documents; no token pair fits. | Same class as call #4 code surfaces. |
| 20 | Extended constellation palette --c-mem6..8 | map.html :root | JS palette had 8 memory colors, only 5 declared as vars — completed the set so JS reads all category colors from CSS. | Completes call #6. |
| 21 | Compact control sizing below spec minimums in the board drawer | map.html board header + idea cards | Drawer-header search ~28px and 19px hover actions kept for density; tokens/focus rings applied. Flagged, not silently resized. | §2.9 sizes assume form contexts; dense toolbar furniture. |
| 22 | Menu-panel padding 16px (below §15's 18-24 card range) | map.html .panel | 230px control popover follows §6 dropdown density, not §15 card padding. | §6 is the nearer anchor. |
| 23 | Board drawer "r6 sections" → `--radius-btn` token (7px) | map.html board | Tokens-only rule wins over the prompt's raw 6px. | §2.9 documents the 6-7 range. |
| 24 | Board cards badge = priority (!high/!med/!low), not status | map.html board | Status IS the column — a status badge would duplicate it; priority uses §20.7 tints (error/warning/neutral). | §20.7. |
| 25 | Board open slides toolbar buttons left of the drawer | map.html | Keeps BOARD toggleable + its active state visible instead of burying the buttons. | §14 states visibility. |
| 26 | Mobile board = full-width takeover (54vh half-sheet removed) | map.html ≤720px | Matches the existing embed behavior; galaxy rAF paused while covered. | §19 mobile TBD — nearest existing pattern. |
| 27 | Hover routing edge starts at the drawer's left edge at card center | map.html canvas | Spec allowed drawer edge or card position; edge reads cleaner across the canvas. | §2.8 motion language. |
| 28 | Hub hover highlights ALL descendant cards (prefix match) | map.html | Direct-children-only felt arbitrary from the map side; descendants match the dim-cluster behavior. | Consistency with existing matches-dimming. |
| 29 | Explorer stays a singleton; shared window chrome extracted instead | index.html P9.3 | The prompt's "cap-4 multi-window explorer" doesn't exist in code — doc windows now share the explorer's drag/snap/resize via one adapter rather than inventing explorer multiplicity. | Program contract: gate decides what exists; P3.12 rolled into P9.3. |
| 30 | Doc-window layer always stacks above the explorer (no z interleave) | index.html | Preserves the old popup's relationship (z96>70); simpler mental model. | §5. |
| 31 | Esc/Ctrl+W target the doc layer only when the last mousedown was in a doc window/chip | index.html | Focus-scoped keys per the pass-2 pattern; clicking explorer/app hands Esc back. | §14 focus discipline. |
| 32 | Old center-pane openFile viewer orphaned as entry point | index.html | Edit/save flows still reference it internally; edit-in-window returns properly in P9.4 Tier-1 editors. | Hand-off, not a loss. |
| 33 | Chip bar wraps to full width, 140px ellipsized labels | index.html | 12 windows = 3 tidy rows; scrolling chip strip felt worse. | §20.7 chips. |
| 34 | Cmd+W not interceptable in Chrome — Ctrl+W everywhere, tooltip documents it | index.html | Browser-reserved shortcut; honest label beats broken promise. | Honest-limits. |
| 35 | **USER OVERRIDE 2026-07-17:** kanban board = BOTTOM sheet, not right drawer | map.html | Vlad reviewed the Phase-K right drawer and rejected it. Reverted to a 40vh bottom sheet keeping all modernized styling + the bidirectional hover-link (edge now starts at the sheet's top edge at the card's x). | User's live word beats the program prompt's drawer spec. |
| 37 | **USER OVERRIDE 2026-07-17:** single-app — nothing opens in a separate browser tab | app-wide | Tool pages (tree/wishlist/queue/memory/activity) become in-app iframe windows with full window chrome; map preview links + open buttons route to parent smartOpen when embedded. ONE sanctioned exception: external http(s) links still open the system browser with ↗ — arbitrary external sites can't render in-app reliably (X-Frame-Options); flag if Vlad wants them blocked instead. | User override, logged verbatim. |
| 38 | Image-viewer backdrop `#0C0C0F` literal in both themes; viewer bar chrome dark-literal | index.html P9.5 | Photo ground must stay dark (§3.3 elevation logic); window chrome itself stays themed. | Spec'd by the program prompt; approved literal. |
| 39 | Filmstrip current thumbnail = accent border only | index.html P9.5 | Selection marker (no text to tint) — selections keep accent per the toggle-override scoping. | §20.7 selection language. |
| 40 | Window-cap retirement never closes a dirty window | index.html P9.4 | May transiently exceed the 12-cap rather than lose unsaved edits. | Data-safety beats the soft cap. |
| 41 | xlsx grid opens read-only; pencil enables editing; edited sheets save as values (formulas collapse, styling dropped — disclosed in-UI) | index.html P9.4 | SheetJS CE limitation; silent formula corruption would be worse — honest label shipped. | Honest-limits. |
| 42 | pdf = native Chromium viewer (pdf.js skipped); pdf/docx view-only + Edit externally | index.html P9.4 | Native renders fine; no lossless in-app editor exists for these formats. | Honest-limits, per spec. |
| 43 | SVG crispness via 8× pre-raster + transform zoom | index.html P9.5 | Keeps the no-layout-thrash rule while staying sharp to 800%. | §2.8 perf. |
| 44 | Notification channel defaults: only `info` is banner-only; every error/attention AND routine-completion type = all three channels | notify-registry.js | The spec's two "on" groups collapse to the same set in practice. | P10 spec interpretation. |
| 45 | Per-row bell read via {at, ids}; popover open no longer auto-marks-all | index.html P10 | readAt alone can't mark one row; auto-mark-all made unread dots invisible. Footer Mark-all keeps old behavior. | §6 anatomy. |
| 46 | Legacy notify.inApp/notify.sound master switches removed from UI (DND + per-type supersede); email rows kept | settings P10 | Values remain in settings.json for compatibility. | No duplicate config surfaces (P10 spec). |
| 47 | server-unreachable type skips bell persistence by design | notify-registry.js | The server is down — nowhere to persist; banner+sound only. | Honest-limits. |
| 48 | process-start legacy event has no v2 type; its sound assignment not migrated | P10 migration | No current emitter; volume preview still uses the playEvent shim. Add a type when something emits it. | Registry completeness rule, deferred. |
| 36 | **USER OVERRIDE 2026-07-17:** all toggles greyscale — never blue accent | app-wide | Segmented controls + toggle chips: active = `--bg-elevated` + `--text-primary` (the §6 theme-toggle pattern), superseding §20.1's accent-fill active segment. Applied: map (6 toggle families), activity chips; index.html seg2/viewseg/rc queued behind the P9.4 file owner. INTERPRETATION: scoped to view/mode toggles + toggle chips; §20.2 switches/checkboxes and active-nav tint states kept — flag if Vlad wants those grey too. | User override, logged verbatim. |
| 49 | **Sidebar rework 2026-07-18:** account status consolidated into ONE header chip (connection dot + usage %), reusing the existing usage chip/popover; removed the separate #connDot and the sidebar account card | index.html header | Task 2 asks for a minimal "dot + % only" chip; keeping the old usage chip + a new account chip would show usage twice. One chip, full detail in the §6 popover (email, plan, connection, Disconnect, 5h/7d bars + resets). | Sidebar-rework task |
| 50 | Header chip shows the 5-hour SESSION % as the single number; border severity from max(5h,7d); the popover carries both windows + resets | index.html loadUsage | Task example is "41%" + "resets in ~2h" (the 5h window). The weekly % and its reset live in the popover breakdown; the border still flags a high weekly. | Sidebar-rework task |
| 51 | Collapse toggle kept (not removed) — absolutely pinned right, opacity:0, revealed on sidebar hover OR button focus-visible; logo recentered by dropping brand-home flex:1 + brand justify-content:center | index.html sidebar | Task 1.2 wants the toggle hidden-until-hover with keyboard-focus reveal (a11y); pinning it absolute stops it shifting the logo's centering axis (Task 1.1). | Sidebar-rework task |
| 52 | "Weekly digest" removed from the dashboard Tools row when it became an Action card (avoid duplication); loadFiles() KEPT (guarded) after the Files-tab removal | index.html dashboard + loadFiles | Recon flagged the Tools/Actions duplicate. loadFiles builds DW.allPaths/state.allFiles consumed by doc-window links, image picker, @-file autocomplete, dwExists — deleting it would break those; only its #paneFiles render is now guarded out. | Sidebar-rework task |
