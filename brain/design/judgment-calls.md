# OS-grade overhaul — judgment calls

> Decisions made where `brain/design/CRS-design-system.md` is silent or conflicts with the run prompt. The design system doc is the judge (autonomy contract §1). Reviewed by Vlad once at the end of the run.

| # | What | Where | Call | Why |
|---|---|---|---|---|
| 1 | "Approved literals §2.11" cited by the run prompt does not exist in the doc | all sweeps | Treat as the approved-literal set: `--white` text on accent/status fills, the §2.6 module-status-tag hexes, §20.7's noted rgba badge fills *until* re-tokenized, and shadow/tint rgba values inside the §2.10 block itself. Everything else must be a token. | Nearest doc anchors: §2.5.1 utility colors, §2.6 tag tables, §20.7 app-drift note. |
| 2 | Run prompt says modal widths "420/440/520" | modals sweep | Doc §5 says width 380–460px typical — doc wins; existing modals normalize into that range (settings modal may exceed as a full workspace modal — logged as its own call if kept). | Autonomy contract: the design system is the judge, not the prompt's from-memory numbers. |
| 3 | Run prompt says input focus = "accent @55% border — no glow rings" | inputs sweep | Doc §2.2/§5/§14: focus = `--input-border`→accent + 3px `--accent-glow` ring. Doc wins. `--accent-border-active` (the 55% rgba) stays for *selected chips/selection borders* per §2.3. | §14 defines Focused = 3px glow ring explicitly. |
| 4 | No tokens exist for code-block surfaces, prose body in rendered docs, or translucent "veil" overlays | index.html app-specific block | Kept as page vars (`--code-bg/--code-fg/--prose/--veil/--veil-2/--head-bg`) with paired light values, marked approved literals. Fixed the broken self-referential `--code-fg`/`--prose` dark values (#D4D4D8 / #C9C9CE ≈ between text-primary/secondary). | §2 has no code/prose tokens; §20.6 table spec nearest but not applicable. |
| 5 | Header theme control | index.html header | Icon-only (moon/sun) segmented at 28px next to the settings gear, reusing the app's `.seg` (active = `--bg-elevated`+`--text-primary` per §6's theme-toggle row; §20.1 smallest size 28). Light active segment gains a 1px-3px shadow per §3.3 elevation. | §6 specs the theme toggle inside the user menu; this app has no user menu — header is the equivalent surface. Labels dropped for header density; aria-labels kept. |
| 6 | Galaxy-map constellation category colors (`--c-mem1..5`, `--c-skill`, `--c-routine`, `--c-app`) | map.html | Kept as page-level data-viz literals in both themes. | No canonical tokens for category/data-viz colors; §2.5 status colors are semantically wrong for node categories. |
| 7 | Map surfaces were an off-palette darker set (#0A0A0C/#141417/#1C1C21) | map.html | Replaced by canonical surfaces via tokens.css aliases — the map now uses `--bg-primary/--bg-secondary/--bg-tertiary` like every other screen. Visual change: map background lightens slightly to #181818. | Inventory verdict NON-COMPLIANT; §2.1 defines the only approved surface set. |
| 8 | Legacy alias token names (`--panel`, `--line2`, `--good`…) kept app-wide | all pages | Aliases stay as documented var() derivations in tokens.css; canonical names required for new code only. A ~700-occurrence mechanical rename was churn without compliance value — every alias resolves to the canonical token per-theme. | §2 requires token *references*, which aliases are; inventory OFF-TOKEN rows reclassified compliant-by-alias. |
| 9 | Accent-as-text carrier | app-wide | The app's accent text/icons ride `--accent2` → `--accent-soft` (#60A5FA dark / #2563EB light, ~4.5:1 AA on white) rather than `--accent-text` (#1D4ED8, 5.8:1). Kept: AA-passing and consistent. New code should prefer `--accent-text` for small text. | §2.3 defines both roles; §18 requires ≥AA — met. Phase-1 verifier finding, ratified. |
