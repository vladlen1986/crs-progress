# Bubble issue report — 2026-08-05

> READ-ONLY pass (`buildprint sync` + `buildprint check` on **casinoreportingsystem/test**). Nothing was applied or changed in the Bubble app — every fix below is a **proposal**. Auto-fix is out of scope pending an approved design.

> ⚠ **sync failed on this machine** — checked the last-synced local state instead. Fix: re-link the CLI (`buildprint link <token>`) or run `buildprint update`. Error: Updating Buildprint CLI from 4.2.7 to 4.3.0...
Buildprint CLI auto-update failed. Run `buildprint update` for details.
Could not reach https://api.buildprint.ai: Unable to connect. Is the computer able to access the url?

## Result: 0 issues — check passed clean.

## Raw check output
```
No changed files. No checks run.
```
