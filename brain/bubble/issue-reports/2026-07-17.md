# Bubble issue report — 2026-07-17

> READ-ONLY pass (`buildprint sync` + `buildprint check` on **casinoreportingsystem/test**). Nothing was applied or changed in the Bubble app — every fix below is a **proposal**. Auto-fix is out of scope pending an approved design.

> ⚠ **sync failed on this machine** — checked the last-synced local state instead. Fix: re-link the CLI (`buildprint link <token>`) or run `buildprint update`. Error: spawn buildprint ENOENT

## Result: 0 issue line(s) flagged

## Raw check output
```
spawn buildprint ENOENT
```

## Proposed fixes (NOT applied)
## Not a Bubble issue — tooling failure

`spawn buildprint ENOENT` = Node couldn't find the `buildprint` executable. The check **never ran** against the TEST branch, so there is nothing to analyze about the Bubble app state.

**What this means:**
- No schema, Option Set, privacy rule, or workflow findings — the CLI didn't touch the TEST worktree.
- Cannot confirm or refute anything about `casinoreportingsystem/test`.
- Any subsequent `buildprint apply` would also fail with the same error.

**Likely causes (in order):**
1. `buildprint` CLI not installed globally, or install broke (npm link removed / global bin missing).
2. `PATH` doesn't include the npm global bin dir in the shell the CRS Brain server spawns (`spawnClaude()` inherits the parent env — if Brain was launched from a shell without the right PATH, spawn fails).
3. The Brain server is spawning `buildprint` by bare name instead of an absolute path, and the child process env lacks the resolution.

**Recommended next step (out of my read-only scope, for you to run):**
- In the same shell that launches CRS Brain: `which buildprint` (or `where buildprint` on Windows).
- If missing → reinstall the CLI.
- If present → the Brain server's spawn env is the problem; log the resolved PATH the server sees.

No Bubble-side action to take until the CLI runs and produces a real check report.
