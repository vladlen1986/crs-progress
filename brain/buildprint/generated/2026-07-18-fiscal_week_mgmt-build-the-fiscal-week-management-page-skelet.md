<!-- generated by brain/engine/gen.js -->
Intent: Build the Fiscal Week Management page skeleton in the App SPA per the tracked design: view routing, base layout containers per the design system, and role gating notes for who may manage fiscal weeks; Task 0 locate-and-report the current as-built state first
Module: fiscal_week_mgmt
Generated: 2026-07-18T08:44:50.191Z
Bundle-SHA256: d864b74a37835ec1a94d775470ed401c21f5c9694f5d483cb18b99bb38d92579
Model: claude-opus-4-8

---

# Buildprint Prompt — Scaffold the Fiscal Week Management page skeleton (routing + base containers + role-gating notes) in the App SPA

**TEST/DEV branch only. Never live. Savepoint: "Before Fiscal Week Management skeleton". Run `buildprint check` after each task. Do not push to live. Apply directly without confirmation.**

## TARGET
- Branch: **TEST** (`~/projects/crs-bubble/casinoreportingsystem/test`; `buildprint sync` first).
- Surface: the single-page SPA shell — **`#App` page**, routed by URL param `v` (decisions.md §Routing 5; design §12.6). Module renders as a reusable in the main area when `v` = this module's route.
  - Module: **Fiscal Week Management** (`id: fiscal_week_mgmt`, `route: fiscal-week`, `section: Admin / Core`, `icon: calendar`, `status: not-started`) — from `modules.json`.
- Savepoint name: `"Before Fiscal Week Management skeleton"`.
- VERIFY: exact `#App` page node name + ID (bundle states the routing decision but gives no page ID). VERIFY: the exact `v` value that maps to this module — is it the `route` string `fiscal-week`, or the module `id`/key? Confirm against how existing modules are wired in `#App` before adding routing.

## EXISTING REUSABLES BY ID
Relevant to placing/rendering this module inside the SPA shell:
- Sidebar reusable — VERIFY: ID not in bundle (STATUS §6 lists it as "Sidebar reusable ✅" with no ID).
- Design System page — `bUfVN0` (style approval gate; any new style must be showcased here).
- User menu — `bUXME` / `bUFck`; User card — `bUCur`; RE Nav Toggle (hamburger) — VERIFY ID.
- `# Dropdown - Single` / `# Dropdown - Multi` — 🟡 (searchable, `search_tokens`) — VERIFY IDs; only relevant if the skeleton needs a picker later.

No reusable exists yet for **Fiscal Week Management itself** (module `status: not-started`; STATUS §3 "🔴 NOT STARTED", only DB 🟡). Recreating an existing element is FORBIDDEN — reuse or `--copy` by id. Per design §13, this module's container should be a Reusable Element named **`# Fiscal Week Management`** (`# Name` convention). Task 0 must confirm no such reusable (or a legacy `RE_`/`RE - ` variant) already exists before creating one.

## STEPS FOR BP
**Task 0 — Locate and report (no changes).** Report exact names + IDs and data types touched, before any edit:
- The `#App` page node + ID; how it reads URL param `v`; the container/group where module reusables render; the existing conditional pattern that shows a module by `v`.
- Whether a `# Fiscal Week Management` reusable (or `RE_Name` / `RE - Name` legacy variant) already exists — if so, STOP and reuse it; do not create.
- The **Fiscal Week** DT (bundle: "22 Fiscal Week") — its fields, and confirm presence/absence of `company` + `property` (DB checklist = `partial`, "Fiscal Week DT + Close/Edit perms exist").
- The existing **Close/Edit** permissions for fiscal weeks (module note: "Close/Edit perms exist") — exact `OS - Permission` entry names + `action_type`/`permission_group`.
- The named Styles the shell already uses for page/card/container surfaces (so Task 2 reuses, not forks).

**Task 1 — Create the module reusable shell.** Create Reusable Element **`# Fiscal Week Management`** (design §13 `# Name`). If cloning an existing module reusable's layout is faster, `buildprint copy … --copy <id>` an existing Admin/Core module reusable (fresh ids minted) rather than hand-building. Empty skeleton only — no data logic yet.

**Task 2 — Base layout containers per the design system.** Inside the reusable, build the standard page scaffold using **named paired Styles** (`Name (Dark)` / `Name (Light)`; build `(Dark)` now, name it so `(Light)` slots in via the element's `bptheme` state):
- Page header: page title (Inter 22–28px / 700, `-0.03em`, `--text-primary`) reading "Fiscal Week Management".
- Working surface container(s): cards/panels on `--bg-secondary`, 1px `--border-default`, `--radius-card` (10px), no shadow (§2.7 card = border only); card padding 18–24px; section vertical rhythm 24–32px (§15).
- Body text default = `--text-secondary` (never `--text-primary` / `#E0E0E0` as body — §2.4, §17).
- All color/background/border transitions = **200ms ease-in** (`--transition-colors`).
- **Zero property-level color conditionals**; theme = full style swap on `dark_theme is "no"` only. Reuse existing shell styles — do not fork near-duplicates. Any genuinely new Style must be showcased on the `design_system` page (`bUfVN0`).
- Approved literals (badge rgba / selected-row tint / overlay ink) stay literal.

**Task 3 — Wire routing.** Add the conditional in `#App` that renders `# Fiscal Week Management` in the main area when URL param `v` = this module's route value (VERIFY exact value, Task 0). Match the existing per-module routing pattern exactly; do not invent a new mechanism.

**Task 4 — Role-gating notes (skeleton, not enforcement).** Document in the reusable (and report) who may manage fiscal weeks, using the permission-based check pattern: `Current User's role's permissions contains [X]` (decisions.md §2, security.md). Use the **existing Close/Edit fiscal-week permissions** located in Task 0 as `[X]`. This task is display/gating scaffolding only — **all enforcement of writes belongs in DATA/PRIVACY + backend (below), not the UI.** VERIFY the exact permission names.

## DATA / PRIVACY
- **Pattern A tenant scoping (quoted verbatim, CLAUDE.md / security.md):** *"Every business Data Type carries BOTH `company` and `property` fields. A user belongs to exactly ONE property. Privacy rules check both: `Current User's company = This Thing's company AND Current User's property = This Thing's property`. No cross-property visibility."* Also verbatim: *"Don't write privacy rules that check only company. Must check company AND property on every business DT."*
- **Fiscal Week DT is currently `public-everyone` (🔴 unconditional read/search)** per security.md tracker — no Pattern A rule, and DB checklist notes `company`/`property` presence is only `partial`. This skeleton must **not** rely on privacy rules as a backstop. If the skeleton surfaces any Fiscal Week records, VERIFY `company` + `property` exist on the DT and add the both-field privacy rule as part of the Pattern A rollout (module `privacy` checklist = `todo`) — but confirm scope with Vlad before applying, as it's a security change beyond this skeleton's stated intent.
- No exception applies here (the only approved Pattern A exception in the bundle is the `User` DT; do not extend it to Fiscal Week).
- **Any write goes through a private, server-guarded backend workflow — no UI-only or auto-bound write** (edit.md Task 3). This skeleton adds no writes; Close/Edit actions, when built, must route through a permission-checked backend workflow. Do not add auto-bound inputs to Fiscal Week.

## WU NOTES
- **Conditional routing must stay on page-local data** — the `v`-param conditional and any theme/`bptheme` condition must key off custom state / URL param / `Current User` fields, never `Do a search for` (bubble-platform-notes, conditional-style-swapping.md: *"keep theme conditions on page-local data … never `Do a search for`"*). Applies to Task 3 routing and Task 2 theming.
- If Task 4's role-gating uses a visibility condition, base it on `Current User's role's permissions contains [X]` (a Current-User field access), not a search.
- No list/`:count` over Fiscal Week records is introduced by this skeleton; if a fiscal-week list is added later, use server-side constraints, not client filters, on large sets (wu-guardrails §1). — *Source: `brain/engine/context/wu-guardrails.md`.*

## MANUAL VERIFICATION
- [NEG] Sign in as a user **without** the fiscal-week Close/Edit permission → confirm the manage controls/notes are hidden and no manage action is reachable (BP applies as app owner and cannot verify a lesser-privileged account).
- [NEG] Sign in as a user in a **different property** (and different company) → confirm no Fiscal Week records from another tenant are visible once privacy is applied; BP's `data`/editor access bypasses privacy rules, so this cross-tenant negative cannot be checked by BP.
- [NEG] With the Fiscal Week DT still `public-everyone`, confirm (via a logged-in other-tenant test user) whether records currently leak cross-tenant — records this must gate before any real-data use.
- [NEG] Confirm the `v`-routing shows the module only for the intended route value and does not render for an unauthorized/blank `v` — verify from a second, lesser-privileged session.
