<!-- generated by brain/engine/gen.js -->
Intent: Read-only combined audit of the User Management module as built on the TEST branch: verify UI, UX, data model, option sets, privacy rules (company AND property scoping), role gating, workflows, and the as-built section composition against the brain's tracked state; produce the scorecard with PASS/FAIL/MISSING per dimension, the machine JSON block, and the [NEG] manual-test list; change nothing
Module: user_management
Generated: 2026-07-18T06:48:46.139Z
Bundle-SHA256: a9b2346946471a792a57d6d7242796bf9f6dc08a4c0474ef5067c24383e145bd
Model: sonnet

---

# Buildprint Prompt — Read-only combined audit of User Management (TEST branch)

**TEST/DEV branch only. Never live. Savepoint: "Before User Management combined audit". Run `buildprint check` after each task. Do not push to live. READ-ONLY — change NOTHING.**

## TARGET

Branch: TEST (casinoreportingsystem, `test` branch worktree). Module under audit: **User Management** (`module_id: user_management`, section: Admin / Core, route: `users`, icon: `person-gear`). Primary data type: **User**. Reusables in scope: User menu (`bUXME` / `bUFck`), User card (`bUCur`), Theme toggle (segmented), Sidebar reusable, `# Dropdown - Single / Multi`, `# People Filter` (two-pane) if referenced from User Management surfaces. Savepoint name: "Before User Management combined audit".

## EXISTING REUSABLES BY ID

- User menu — `bUXME` / `bUFck`
- User card — `bUCur`
- Design System page — `bUfVN0` (canonical style reference; use for cross-checking named-style compliance)
- Theme toggle (segmented) — no ID given in bundle → VERIFY: what is the element ID for the Theme toggle reusable?
- RE Nav Toggle (hamburger) — no ID given in bundle → VERIFY: what is the element ID for RE Nav Toggle?
- `# Toggle - Status`, `# People Filter` (two-pane), `# Dropdown - Single / Multi` — no IDs given in bundle → VERIFY: what are the element IDs for these three reusables?
- Sidebar reusable — no ID given in bundle → VERIFY: what is the element ID for the Sidebar reusable?

This is a **read-only audit** — no cloning, no `--copy`, no new reusables. Task 0 must still resolve the IDs flagged VERIFY above by locating them in the synced workspace before reporting on them.

## STEPS FOR BP

0. **Locate-and-report (mandatory first task, no changes):** Resolve exact element/reusable/style names + IDs for every User Management surface (page/reusable for `users` route, User menu, User card, any list/card/detail/table view elements) and the data types touched (`User`). Report the `dark_theme` vs `theme` field question left open in design.md §3.2/`bptheme` doc — record which one the User DT actually carries.

1. **UI dimension:** Verify presence of the 7 KPI tiles (click-to-filter), 4 views (list/card/detail/table, one toggle), profile header, alphabet rail, filter chips — per STATUS.md §3 row 1 (confirmed ✅ 2026-06-11). Report PASS/FAIL per sub-element against that baseline; flag any drift since 2026-06-11.

2. **UX dimension:** Verify click → quick-view drawer, "View Profile" → full page, mobile filter sheet + overview collapse — per STATUS.md §3 row 2 (✅). Report PASS/FAIL; flag the tracked issue "mobile 'Filters' button non-functional" (STATUS.md flags list) — confirm still broken or resolved.

3. **Data model dimension:** Verify User DT carries `role` (single) + `property` (direct), confirm it is the real User DT (not `ZZ_UI_DemoUser`) — per STATUS.md §3 row 3 (✅, replaced Jul). Report PASS/FAIL. Flag the tracked "22 demo records to purge" item — confirm count and presence (read-only `buildprint data` query only, no delete).

4. **Option sets dimension:** STATUS.md §3 row 4 marks this ➖ (not applicable) for User Management. Report as MISSING/N-A per tracker, do not fabricate an option-set requirement.

5. **Privacy rules dimension (Pattern A):** Quote and check against the locked rule verbatim: "Current User's company = This Thing's company AND Current User's property = This Thing's property." Per security.md tracker, User DT is **property-only** and is a **resolved, approved Pattern A exception** (decisions.md 2026-07-16) — property-only pins company transitively (Property→Company single-parent, one user = one property); this is NOT a gap, do not re-flag it as non-compliant. Report the User DT's actual privacy_role as read from the synced workspace; PASS if it matches "Same property as current user" + no company field; FAIL/MISSING otherwise. Cross-check against STATUS.md §3 row 7, which currently shows 🔴 "not built" — flag this as a **discrepancy between security.md's 2026-07-16 exception resolution and STATUS.md's still-red row 7**, dated 2026-07-15 (STATUS.md predates the exception decision by one day). Do not resolve the discrepancy yourself — report it.

6. **Role/permission gating dimension:** Verify `users___view` page-visibility gate is wired (STATUS.md §3 row 6, 🟡). Report PASS/FAIL. Note permission model is locked: "Custom Role DT + fixed Permission OS + per-user extras list" (CLAUDE.md, decisions.md 2026-04-17) — check gating logic matches this shape, not a pure role-based check.

7. **Workflows dimension:** Verify CRUD workflow state — UI-state + read searches done (🟡); create-with-temp-password and deactivate are Pass-2/未built per STATUS.md §3 row 11. Verify backend/API WF (row 12, 🔴) and WF permission guards (row 10, 🔴) — confirm create/reset/deactivate are NOT yet server-guarded. Report PASS/FAIL/MISSING per sub-item; do not treat any 🔴/🟡 item as done without direct verification in the synced workspace.

8. **Theme dimension:** Verify dark + light with ~280 conditionals (STATUS.md §3 rows 9/16, ✅). Cross-check against the LOCKED paired-styles pattern: named styles `Name (Dark)` / `Name (Light)`, single conditional per element swapping the whole style via `bptheme` state, zero property-level color conditionals. Report PASS if the ~280 conditionals are whole-style swaps per element (not per-property); FAIL if any inline/literal colors or property-level theme conditionals are found (approved literals — badge rgba, selected-row tint, overlay ink — stay literal and are not a fail). Flag "restore dark after light checks" note from STATUS.md row 16.

9. **Section composition cross-check:** Confirm User Management is listed in Admin / Core per the 46-module lock (decisions.md 2026-04-28 final lock). Confirm icon is `person-gear` (post icon-swap, resolves collision with Visitor Management) and route is `users`. Report PASS/FAIL against `modules.json`.

10. **Audit trail dimension:** Verify audit logging (STATUS.md §3 row 14, 🔴) against the locked architecture: "Single `ActivityLog` DT, polymorphic `subject_type`/`subject_id`, async write via Schedule API Workflow." Report MISSING if not found.

11. **Empty/Loading/Error states:** Verify per STATUS.md §3 row 15 (🟡, loading animations built) — report PASS/PARTIAL/FAIL for empty and error states specifically, since only loading is confirmed built.

12. **Run `buildprint audit`** and cross-reference any findings scoped to the `User` DT or User Management surfaces against the 45-high inventory in security.md (39 public-data-types, 5 public-uploaders, 1 missed-server-side-redirect). Report whether `User` DT appears among current findings.

## DATA / PRIVACY

Tenant-scoping rule (verbatim, Pattern A): "Every business DT's privacy rule checks `Current User's company = This Thing's company AND Current User's property = This Thing's property`." **User DT exception (verbatim, decisions.md 2026-07-16):** "property-only pins company transitively because Property→Company is single-parent and a user belongs to one property" — approved, do not re-flag. Precondition per that exception: `User.property` must stay required + single-valued — verify this precondition still holds as part of Task 5.

This is a **read-only audit** — no writes of any kind. Any workflow found performing writes (create-with-temp-password, deactivate, reset) must itself be routed through a private server-guarded backend workflow per standing convention: "if a write is involved, route it through a private server-guarded backend workflow — do not add a UI-only or auto-bound write." Report only whether this is currently true; do not modify.

`buildprint data` reads bypass privacy rules (editor access) — treat every row read during this audit as sensitive; do not paste raw PII (User DT may carry personal fields) into the report beyond what's needed to state PASS/FAIL.

## WU NOTES

- **Server-side constraints beat filters** (`brain/engine/context/wu-guardrails.md` §1): if the audit inspects the User list/table/KPI-tile searches, confirm constraints (e.g. `property =`, `company =`) are applied server-side in the search, not via client-side filter on a downloaded list.
- **Client-side filtering caveat** (§2): the alphabet rail and filter chips, if implemented as client-side filters over a loaded User list, are only acceptable under "no more than a few hundred items, simple filters, non-sensitive data" — User records carry PII per security.md's Employee-DT-adjacent concern, so flag if User lists are filtered client-side without a prior server-side scope constraint.
- **`:count` behavior** (§3): if any of the 7 KPI tiles use `:count` on an unconstrained or list-based search, verify it's a proper server-side search-based count, not a list-based `:count` vulnerable to stale-record errors.
- These guardrails apply to Tasks 1 (KPI tiles), 3 (data model / demo record count check), and 12 (audit cross-reference).

## MANUAL VERIFICATION

- [NEG] As a lesser-privileged (non-admin) user account in Property A, attempt to view/search User Management records belonging to Property B within the same company — confirm no cross-property visibility.
- [NEG] As a logged-in user without the `users___view` permission grant, attempt to navigate directly to the `users` route via URL param `v` — confirm the page-visibility gate blocks rendering (not just hides nav links).
- [NEG] As an unauthenticated (logged-out) user, attempt to load the `App` page directly on the `users` route — confirm redirect happens server-side before content renders (cross-check against the tracked `missed-server-side-redirects` finding for the `app` page).
- [NEG] As a lesser-privileged user, attempt to trigger create-user, reset-password, or deactivate-user actions directly (e.g. via browser devtools / API call) bypassing the UI — confirm these are backend-workflow-guarded and reject unauthorized calls (STATUS.md flags rows 10/12 as 🔴, so this is expected to currently FAIL — confirm and document the failure mode precisely).
- [NEG] Attempt to read another company's User records via the exposed Data GET API (`exposes_get_api: true` at app level, per security.md) using a non-admin authenticated session — confirm whether privacy rules block this given User DT is property-only (transitively company-scoped).
- [NEG] Toggle theme as a non-admin user and confirm no privilege or data leakage occurs via theme-conditional elements (i.e., confirm theme conditionals never gate data visibility, only styling).
