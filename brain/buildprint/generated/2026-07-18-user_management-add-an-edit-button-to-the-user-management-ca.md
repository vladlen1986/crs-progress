<!-- generated by brain/engine/gen.js -->
Intent: Add an Edit button to the User Management card view that opens the existing user profile popup in edit mode
Module: user_management
Generated: 2026-07-18T01:35:12.911Z
Bundle-SHA256: a9b2346946471a792a57d6d7242796bf9f6dc08a4c0474ef5067c24383e145bd
Model: sonnet

---

# Buildprint Prompt — Add Edit button to User Management card view, opening the user profile popup in edit mode

**TEST/DEV branch only. Never live. Savepoint: "Before Add Edit button to User Management card view". Run `buildprint check` after each task. Do not push to live. Apply directly without confirmation.**

## TARGET

- Branch: **TEST**.
- Page/reusable: **User Management** module (Admin / Core section) — card view (one of the module's "4 views (list/card/detail/table) one toggle", per `brain/STATUS.md` §3 row 1). Exact reusable/element name + ID: **VERIFY: what is the exact Bubble element/reusable name and ID for the User Management card-view item, and for the existing "user profile popup"?** — the bundle documents a "quick-view drawer" and a "View Profile" full page (`brain/STATUS.md` §3 row 2) but does not name a popup element, and does not confirm it currently supports an edit mode.
- Savepoint name: `"Before Add Edit button to User Management card view"`.

## EXISTING REUSABLES BY ID

The bundle lists no reusable matching "user profile popup" or a User Management card-view item by ID. Relevant named reusables that ARE in the bundle (`brain/STATUS.md` §6):

- User menu (bUXME / bUFck) — not the target, listed for context only.
- User card (bUCur) — sidebar-bottom trigger, not the card-view item.
- Design System page (bUfVN0) — canonical style reference; any new style must be showcased here per Convention family 5.

**No relevant reusable for the card-view item or the profile popup is confirmed by ID in the bundle. Recreating an existing element is FORBIDDEN — Task 0 below must locate the exact IDs before any change; do not build a new popup if one already exists.**

## STEPS FOR BP

0. **Locate-and-report** (no changes): report the exact element name + ID of the User Management card-view reusable/component; the exact element name + ID of the existing user profile popup (if any exists) and whether it currently has any edit-mode state/inputs; the data type(s) touched (User DT fields to be made editable). Confirm whether an edit-capable popup already exists before building anything new.
1. Add an **Edit** button/icon to the card-view item (Feather icon per bundle §16 iconography rule; ghost/icon-button styling consistent with existing card-view controls found in Task 0).
2. Wire the Edit button to open the existing user profile popup with a state/parameter indicating edit mode (e.g. a custom state on the popup, set from the triggering workflow) — do not fork a duplicate popup. If Task 0 finds the popup has no edit-mode support, add the minimum inputs/state needed, reusing the popup's existing structure.
3. Style per the bundle's conventions: named paired styles `Name (Dark)` / `Name (Light)`; theme = full style swap on `dark_theme is "no"` only (per `brain/bubble/conditional-style-swapping.md`, field name under review — confirm in Task 0 whether the app uses `dark_theme` or `theme`); zero property-level color conditionals; approved literals stay literal; reuse existing styles — do not fork near-duplicates.
4. Wire the save action in edit mode to write to the User DT through the appropriate write path (see DATA / PRIVACY below) — do not add a UI-only or auto-bound write.

## DATA / PRIVACY

- The User DT's privacy rule is a documented **Pattern A exception**: *"the `User` DT's property-only rule is now an **approved Pattern A exception**, not a gap (decisions.md 2026-07-16) — property-only pins company transitively because Property→Company is single-parent and a user belongs to one property. The remaining property-only rows are still under review. Do not re-flag User as non-compliant."* (`brain/security.md`)
- Precondition tied to that exception: *"Precondition: `User.property` must stay required + single-valued"* (`brain/security.md`) — do not let the edit form allow clearing or multi-valuing `property`.
- Any write goes through a private server-guarded backend workflow — no UI-only or auto-bound write (Convention family 3, `templates/edit.md` Task 3). Note the bundle records User Management's backend/API write path as **not yet built**: *"12 | Backend/API WF | 🔴 | Pass-2"* and *"10 | WF permission guards | 🔴 | Pass-2 (create/reset/deactivate must be server-guarded)"* (`brain/STATUS.md` §3) — flag this: **VERIFY whether a server-guarded update workflow for User fields already exists to route this edit-save action through, or whether Task 0 must locate/build one before wiring Task 4.**

## WU NOTES

No WU-relevant surface in this change — the bundle's WU guardrails (`brain/engine/context/wu-guardrails.md`) address server-side search constraints, client-side list filtering, `:count` behavior, and denormalized fields; this change adds a button and opens an existing popup against a single already-loaded User record, not a new search or list.

## MANUAL VERIFICATION

- [NEG] As a lesser-privileged / non-permitted user (no user-edit permission granted on their Role), confirm the Edit button is hidden or the save action is rejected server-side — the bundle records `10 | WF permission guards | 🔴 | Pass-2` and `12 | Backend/API WF | 🔴 | Pass-2`, meaning this protection is not confirmed built.
- [NEG] As a user from a different property, attempt to open/save an edit on a User record belonging to another property and confirm it is blocked, given `User.property` is the sole scoping field (Pattern A exception) — verify the precondition (`property` required + single-valued) still holds after this edit path is added.
- [NEG] Confirm the edit-mode popup does not expose or allow editing of fields beyond the User DT's intended editable set (the bundle flags `06 Employee` PII exposure as the worst existing finding — verify no equivalent unintended field exposure is introduced on the User popup).
