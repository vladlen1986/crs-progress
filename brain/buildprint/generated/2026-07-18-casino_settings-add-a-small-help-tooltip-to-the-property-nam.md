<!-- generated by brain/engine/gen.js -->
Intent: add a small help tooltip to the property name field
Module: casino_settings
Generated: 2026-07-18T13:25:01.917Z
Bundle-SHA256: a4856aee5c8a222e01fbe25070e77ef0da20b2943e2e69a405a32e3f593310f8
Model: claude-opus-4-8

---

# Buildprint Prompt — Add a help tooltip to the Property name field (Casino Settings)

**TEST/DEV branch only. Never live. Savepoint: "Before Property name tooltip". Run `buildprint check` after each task. Do not push to live. Apply directly without confirmation.**

## TARGET
- **Branch:** TEST (`casinoreportingsystem` Test worktree).
- **Reusable being changed:** `# Casino Settings` — the Core/Settings reusable element (module `casino_settings`, route `settings`, status `done`). **ID not in bundle → Task 0 must resolve it.**
- **Surface:** the Property detail **Identity** card's name field (Property DT). This is a display-only tooltip addition — no field, DT, or workflow change.
- **Savepoint name:** `"Before Property name tooltip"`.

## EXISTING REUSABLES BY ID
- Design System page — `bUfVN0` (approval gate for any new style; canonical `crs-design-system.html`).
- `# Casino Settings` — **ID not present in the bundle. VERIFY: exact element ID of `# Casino Settings` (Task 0).**
- No dedicated "tooltip" reusable is listed in the bundle (STATUS.md §6 lists User menu, User card, Theme toggle, Nav Toggle, `# Toggle - Status`, `# People Filter`, `# Dropdown - Single/Multi`, Sidebar — none is a tooltip). **VERIFY (Task 0): does a tooltip/help reusable or a Bubble native tooltip pattern already exist in the app? If yes, reuse it; if no, build the tooltip inline on the Identity card — do not create a new reusable without confirmation.** Recreating an existing element is FORBIDDEN — reuse it, or clone by id (`--copy`).

## STEPS FOR BP

**Task 0 — Locate & report (READ-ONLY, no change):**
- Resolve and report, with exact names + IDs:
  - The `# Casino Settings` reusable element ID.
  - The Property detail **Identity** card group and the Property **name** input/label element (name + ID) that the tooltip attaches to.
  - The data type touched: **Property** (confirm the name field's element; the tooltip reads no new data).
  - Any existing tooltip/help element, icon element, or reusable already in the app (search both cases — `file`/`find` are case-sensitive) and the icon element pattern used elsewhere.
  - The named styles already applied on the Identity card labels/icons (so the tooltip reuses them, not a fork).
- Report all of the above before any edit. **Do not proceed to Task 1 until IDs are confirmed.**

**Task 1 — Add the help trigger (icon) next to the Property name field:**
- Add a small help icon adjacent to the Property name label/input inside the Identity card.
- Icon: **Feather first** (a Feather `help-circle` fits; confirm availability in the Bubble Icon element). Fall back to a Bootstrap outline icon only if Feather lacks a fit, and note which library. Sizing 13–15px inline; color `--text-muted` default, `--accent-text` on hover/active. (§16)
- Icon must sit on a **named paired style** (`Name (Dark)` / `Name (Light)`); build the `(Dark)` variant now so `(Light)` slots in later via the element's `bptheme` theme state. No inline/literal hex. Color transitions 200ms ease-in. Reuse an existing icon/muted-text style if one already matches — do not fork a near-duplicate. (§3.2, conventions §4–6)

**Task 2 — Show the help text on hover/focus:**
- On hover/focus of the icon, reveal a small tooltip bubble with the help copy.
- Tooltip container styling per the bundle's elevation rules: surface `--bg-secondary`/`--bg-elevated`, `--border-default`, and elevation via `--shadow-dropdown` (in light, elevation = shadow, not brightness — do not mirror dark relationships). Text: message 12px `--text-secondary`. All on named paired styles, zero property-level color conditionals — theme = full style swap on `dark_theme is "no"` only. (§2.1 ⚠, §3.3, conventions §5)
- **VERIFY: exact tooltip help copy** — the intent does not specify the wording, and the bundle contains no help text for the Property name field. Do not invent copy; confirm the string with Vlad before applying.
- **If any brand-new style is created**, it must be showcased on the `design_system` page (`bUfVN0`) per the styling standard.

*Each task independently verifiable: Task 1 = icon renders in the right place on a named style; Task 2 = tooltip reveals on hover/focus with correct surface/shadow and named paired styles.*

## DATA / PRIVACY
This change is display-only — it adds no field, reads no new data, and performs **no write**, so it introduces no new tenant-scoped query. The Property records shown here remain governed by the module's existing, NEG-proven Pattern A rules; do not weaken or bypass them.

Pattern A rule text (applies to the Property DT this card displays), quoted verbatim:
> Every business Data Type carries BOTH `company` and `property` fields. A user belongs to exactly ONE property. Privacy rules check both: `Current User's company = This Thing's company AND Current User's property = This Thing's property`. No cross-property visibility.

If (against this intent) any task ends up introducing a write, it must go through a **private server-guarded backend workflow** — no UI-only or auto-bound write. (Casino Settings already routes its 3 writes via private guarded WFs; do not add a new one for a tooltip.)

## WU NOTES
No WU-relevant surface in this change. The tooltip is static help text triggered by hover/focus — it runs no `Do a search for`, no `:count`, and no client-side filter, so none of the bundle's WU guardrails (server-side constraints, load-once client filter, `:count`, denormalized fields) are touched. Keep the tooltip's visibility on page-local state (hover/focus), never on a search. *(Source: wu-guardrails.md — none applicable.)*

## MANUAL VERIFICATION
- [NEG] As a lesser-privileged / non-Settings user (a role whose permissions do not include the Casino Settings surface), confirm the tooltip and Property name field render only where the existing permission gate allows — the added icon must not expose the Identity card to a user who couldn't already see it.
- [NEG] As a user in a **different property** (and a **different company**), confirm no cross-tenant Property name or Identity data becomes visible through this card — the tooltip must not surface any Property outside the viewer's own `company`+`property`. Buildprint (app owner) bypasses privacy rules, so this cross-tenant negative must be checked manually with a scoped test user.
- [NEG] In **light mode** (`dark_theme is "no"`) as a normal user, confirm the icon and tooltip swap to their `(Light)` styles (shadow-based elevation, darker accent-text) and do not stay dark — an element left on an inline color would fail this.
