<!-- generated by brain/engine/gen.js -->
Intent: chunk c3 casino-settings: Components — Sidebar
Module: casino_settings
Generated: 2026-07-18T08:41:28.690Z
Bundle-SHA256: cb9cc4269e28ac6f38a1c3ba50fc47e3db4651dceb9b4c4cd92b5fa146928b03
Prototype-SHA256: dd65bf739ecfb0ec006307238619fe693f2436df0237c24a96475bfd7990596a
Chunk-Id: c3
Model: claude-opus-4-8

---

# Buildprint Prompt — Reuse the Sidebar reusable to realize casino-settings chunk c3 (sidebar section/title/item/active)

**TEST/DEV branch only. Never live. Savepoint: "Before c3 Sidebar reuse". Run `buildprint check` after each task. Do not push to live. Apply directly without confirmation.**

## TARGET
- Branch: **TEST** (`~/projects/crs-bubble/casinoreportingsystem/test`), never live.
- Surface being changed: the **`# Casino Settings`** reusable element (module `casino_settings`, route `settings`, section Admin / Core) — chunk c3 wires its sidebar surface to the **existing Sidebar reusable** rather than building new sidebar elements.
- Reused component: **Sidebar reusable** (listed ✅ in brain/STATUS.md §6 "theme-aware, mobile overlay + scrim") — VERIFY: exact reusable name (`# …`) and element ID; the bundle gives no ID for it.
- Savepoint name: `"Before c3 Sidebar reuse"`.

## EXISTING REUSABLES BY ID
From the bundle (brain/STATUS.md §6). The bundle lists these with IDs; the Sidebar itself is listed **without** an ID, so Task 0 must resolve it.

- Sidebar reusable — **VERIFY: ID not in bundle** (STATUS.md §6 row "Sidebar reusable ✅"). This is the component c3 must reuse/clone by id (`--copy`) — recreating it is FORBIDDEN.
- Design System page — `bUfVN0` (approval gate for all styles).
- User menu — `bUXME` / `bUFck`.
- User card — `bUCur`.

Every c3 mapping row (`/*`, `.sidebar-section`, `.sidebar-section-title`, `.sidebar-item`, `.sidebar-item.active`) maps to the **same** Sidebar reusable with the instruction **"reuse/clone by ID — never recreate."** Do not build new sidebar elements or styles.

## STEPS FOR BP

**Task 0 — Locate and report (no changes).**
Report, with exact names + IDs + data types touched, before any edit:
- The Sidebar reusable: exact `# Name` and element ID (resolve the VERIFY above via `buildprint summary` / `find`).
- Whether `# Casino Settings` already places a Sidebar instance; if so, its instance ID.
- The existing named styles the Sidebar's section/title/item/active states already use (so nothing is re-forked). Per the mapping these should resolve to the c1/c2 style base (`CRS - /*`) and tokens: `.sidebar-item` → `--text-secondary` / `--transition`; `.sidebar-section-title` → `--text-muted`; `.sidebar-item:hover` and `.sidebar-item.active` → bg `--bg-tertiary`, text/icon `--text-primary`, active accent per design.md §10 (`--accent-text`, **no border** — "pick two").
- Confirm the produced names required by later chunks (`/*`, `sidebar-section`, `sidebar-section-title`, `sidebar-item`, `sidebar-item.active`) will be satisfied by the reused Sidebar's existing structure, or flag the gap.
STOP and report; get go-ahead before any apply.

**Task 1 — Reuse the Sidebar, do not recreate.**
Place / confirm the existing Sidebar reusable inside `# Casino Settings` (reuse the instance, or clone by id with `--copy` only if a distinct copy is genuinely required). Do **not** author new `.sidebar*` elements or styles — the mapping row instruction is "reuse/clone by ID — never recreate," and c3 SCOPE OUT says **no states/conditionals yet, no workflows**.
Verifiable independently: the Casino Settings reusable renders the Sidebar; no new sidebar-named styles were created; `buildprint check` passes.

Per design.md §10 (do not restate as new work — this is what the reused component already encodes): sidebar width 256px, section headers uppercase 11px/700 `--text-muted` **no icons**, nav item 32px, active = bg `--bg-tertiary` + `--accent-text`, **no border**.

Styling discipline: named paired styles `Name (Dark)` / `Name (Light)`; theme = full style swap on `dark_theme is "no"` only; **zero** property-level color conditionals; approved literals stay literal; reuse existing styles — do not fork near-duplicates. c3 adds **no** conditionals (SCOPE OUT), so no `bptheme`/theme rules are added in this chunk.

Note the three UNRESOLVED tokens in the mapping (`--status-error`, `--status-success`, `--status-warning`) are **not** used by any sidebar row — they belong to badge/toast/label rows outside this chunk. Do not resolve or touch them here.

## DATA / PRIVACY
No data reads/writes and no new Data Types in c3 (SCOPE OUT: no workflows; the sidebar is presentational nav). Tenant-scoping rule still governs any business DT the module touches elsewhere — Pattern A, quoted verbatim from CLAUDE.md:

> Privacy rules check both: `Current User's company = This Thing's company AND Current User's property = This Thing's property`.

Named exception (do not re-flag): `User` DT is an approved property-only Pattern A exception (decisions.md 2026-07-16). No writes in this chunk; if any write were ever added it must route through a private server-guarded backend workflow — no UI-only or auto-bound writes.

## WU NOTES
No WU-relevant surface in this change. c3 only reuses an existing presentational Sidebar reusable (no searches, no `:count`, no client-side filtering, no list bindings introduced) — the WU guardrails in `brain/engine/context/wu-guardrails.md` (server-side constraints, load-once client filter, `:count`, denormalized fields) have nothing to bind to here.

## MANUAL VERIFICATION
- [NEG] Sign in as a **lesser-privileged / non-super-admin** user of the **same** company+property and confirm the Casino Settings sidebar renders identically (nav is presentational; no per-permission row exposure was introduced by c3).
- [NEG] Sign in as a user of a **different company/property** and confirm the Casino Settings page/Sidebar exposes no cross-tenant data through any bound sidebar element (should be none — c3 binds no business data; flag if any appears).
- [NEG] Confirm no **dead nav links** were exposed by the reused Sidebar for destinations not yet built (design.md §6 build discipline: stub-hidden until destination exists) — checked as the app owner and again as a lesser-privileged account.

---

## MAPPING CONTRACT (verbatim from prototypes/casino-settings/mapping.md — do not reinterpret)

Produced names — create EXACTLY these, byte-for-byte:
- `/* element`
- `sidebar-section element`
- `sidebar-section-title element`
- `sidebar-item element`
- `sidebar-item.active element`

Tokens used:

| var in file | canonical §2 token | dark | light | used by |
|---|---|---|---|---|
| `--accent` | `--accent` | #3B82F6 | #3B82F6 | .sidebar-item.active, .tab.active, .input:focus, .select:focus, .textarea:focus, .input-with-prefix:focus-within +4 |
| `--accent-active` | `--accent-active` | #1D4ED8 | #1D4ED8 | .btn-primary:active |
| `--accent-glow` | `--accent-glow` | rgba(59,130,246,0.13) | rgba(59,130,246,0.13) | .input:focus, .select:focus, .textarea:focus, .input-with-prefix:focus-within, .property-list-item.selected |
| `--accent-hover` | `--accent-hover` | #2563EB | #2563EB | .btn-primary:hover |
| `--bg-elevated` | `--bg-elevated` | #2A2A2A | #FFFFFF | .btn:hover, .property-avatar, .logo-upload, /* Inline edit toggle */ .save-bar +1 |
| `--bg-primary` | `--bg-primary` | #181818 | #FAFAFA | html, body |
| `--bg-secondary` | `--bg-secondary` | #1E1E1E | #FFFFFF | /* Sidebar */ .sidebar, .topbar, /* Cards */ .card, .input:disabled, .select:disabled, .textarea:disabled +1 |
| `--bg-tertiary` | `--bg-tertiary` | #242424 | #F4F4F5 | .sidebar-item:hover, .sidebar-item.active, .tab-count, .input, .select, .textarea +6 |
| `--border-active` | `--border-active` | #3D3D3D | #C8C8CB | .toggle-slider, .property-detail-empty, .logo-upload, /* Inline edit toggle */ .save-bar +1 |
| `--border-default` | `--border-default` | #242424 | #EAEAEB | /* Sidebar */ .sidebar, .logo, .topbar, /* Tabs */ .tabs +9 |
| `--border-hover` | `--border-hover` | #333333 | #DCDCDE | .input:hover, .select:hover, .textarea:hover, .btn:hover, .property-list-item:hover |
| `--radius-badge` | `--radius-badge` | 5px | 5px | /* Badge */ .badge |
| `--radius-btn` | `--radius-btn` | 7px | 7px | /* Buttons */ .btn |
| `--radius-card` | `--radius-card` | 10px | 10px | /* Cards */ .card, .property-list-item, .property-detail-empty |
| `--radius-input` | `--radius-input` | 7px | 7px | .input, .select, .textarea, .input-with-prefix |
| `--status-error` | **UNRESOLVED** | — | — | .label-required::after |
| `--status-success` | **UNRESOLVED** | — | — | .badge-success, /* Toast */ .toast, .toast svg |
| `--status-warning` | **UNRESOLVED** | — | — | .badge-warning |
| `--text-disabled` | `--text-disabled` | #565656 | #B6B6BB | .input:disabled, .select:disabled, .textarea:disabled |
| `--text-muted` | `--text-muted` | #6B6B6B | #8E8E95 | .sidebar-section-title, .breadcrumb-sep, .card-subtitle, .label-optional +9 |
| `--text-primary` | `--text-primary` | #E0E0E0 | #18181B | html, body, .sidebar-item:hover, .sidebar-item.active, .breadcrumb-current +13 |
| `--text-secondary` | `--text-secondary` | #A6A6A6 | #5F5F66 | .logo, .sidebar-item, .breadcrumb, .page-subtitle +6 |
| `--transition` | `--transition` | 160ms cubic-bezier(.4,0,.2,1) | 160ms cubic-bezier(.4,0,.2,1) | .sidebar-item, .tab, .input, .select, .textarea, .input-with-prefix +5 |

Chunk rows:

| `/*` | Sidebar (CRS-design-system.md §10) | Sidebar reusable — brain/STATUS.md §6 | reuse/clone by ID — never recreate |
| `.sidebar-section` | Sidebar (CRS-design-system.md §10) | Sidebar reusable — brain/STATUS.md §6 | reuse/clone by ID — never recreate |
| `.sidebar-section-title` | Sidebar (CRS-design-system.md §10) | Sidebar reusable — brain/STATUS.md §6 | reuse/clone by ID — never recreate |
| `.sidebar-item` | Sidebar (CRS-design-system.md §10) | Sidebar reusable — brain/STATUS.md §6 | reuse/clone by ID — never recreate |
| `.sidebar-item.active` | Sidebar (CRS-design-system.md §10) | Sidebar reusable — brain/STATUS.md §6 | reuse/clone by ID — never recreate |

