# Resume point — 2026-07-18 (Mac session pause → continue on Windows)

> Written when Vlad paused to continue on the Windows PC. Read this + `brain/qa/program-progress.md` + `brain/qa/master-checklist.md` first. App: `cd crs-brain && npm install && node server.js` (port 4317; `start.bat` on Windows). Governance for all programs: `brain/qa/program-prompt.md` (Steps 2+3), `brain/qa/program3-prompt.md` (Brain Program 3), `brain/qa/polish-prompt.md` (polish pass).

## Where everything stands — ALL THREE PROGRAMS + POLISH ARE BUILT & GATED

- **Steps-2+3 program (Phases 0–10): COMPLETE.** Every phase gated by an independent verifier, two-green loop closed. Checkpoints in `brain/qa/checkpoints/phase-0..10.md`. Phase 4 (Buildprint CLI bridge) is an honest ENV-LIMITED stop (CLI Unauthorized on this Mac); Phases 5–6 used the copy path.
- **Executive polish pass (6 tasks): COMPLETE.** Gate PASS 8/8, two-green loop closed. Checkpoint `brain/qa/checkpoints/polish.md`. Tree rows stripped, executive detail, single theme toggle + neutral chip, kanban→right-drawer (kb-* deleted, one kanban), sidebar pins/⋮/icons, window-policy openTarget router. judgment-calls #56–#59.
- **Brain Program 3 (5 phases): all 5 phases BUILT & GATED.** Checkpoints `brain/qa/checkpoints/p3-phase1..5.md`.
  - P1 Chat v2: operator slash-commands (/settle /chunk /send /todo /decide /gen /help), focus-module context injection, BP auto-tag. **Caught+fixed a real abort bug** (Stop was cosmetic — req.close→res.close across chat/ingest/bp-track).
  - P2 ⌘K palette: /api/search global index (mtime-incremental) + palette extension (kind-grouped, > command mode, recent/pinned).
  - P3 entity pages + backlinks: /api/entity (7 sections) + /api/backlinks (docs/chats/reports) + entity.html surface + auto-linking chips + doc-window backlinks strip. Caught+fixed 2 bugs (chip null-closure; tree m/m0 that broke ALL module expansions).
  - P4 cost intelligence: usage-log.jsonl (per-run token/model/cost at every touchpoint) + /api/usage-analytics + cost.html (§20.4 tiles + SVG trend chart) + routing audit.
  - P5 hardening: client+server error capture (3-repeat→todo) + audit.jsonl (via:remote) + auto-backup (clean-tree daily push + weekly git-archive snapshot keep-8, restore drill proven). **Monolith split = PARTIAL by design** (judgment #59): chat-media.js + palette.js extracted as build-less classic region modules (identical behavior verified); the full 366-fn extraction is a documented continuation (not attempted in one pass to protect the green system).

## FINAL LOOP — DONE. Program complete.

**Brain Program 3 FINAL LOOP: BOTH GREENS ACHIEVED.** Cycle 1 (commit da644bd) and cycle 2 both CLEAN over unchanged app code — 167 PASS / 12 ENV-LIMITED / 0 FAIL / 0 UNTESTED over 179 rows, identical distribution. `git diff` between them = data-churn only, no app-code changes. **The two-consecutive-greens goal is MET; all three programs + the polish pass are complete.**

There is NOTHING left to run on resume — the only open items are Vlad's 12 ENV-LIMITED homework (below). On resume, just `git push origin main` from an authenticated shell and pick up the homework.

## Checklist truth (at pause)

179 rows: **167 PASS · 12 ENV-LIMITED · 0 FAIL · 0 UNTESTED** (both final-loop greens merged). The 12 ENV-LIMITED = Vlad's only homework (see below).

## Vlad's homework (the 12 ENV-LIMITED + standing items)

1. **`buildprint link <token>`** on whichever machine (token from Buildprint web app → Integrations → CLI) — clears BRIDGE.2/3, P1-promptgen.3, P8-sounds-wishlist.7, and unblocks the Phase-4 bridge + PILOT.3/P6.2 report capture.
2. Run the pilot chunk + UM audit + FWM kickoff prompts in Buildprint (copy from the panel / brain/buildprint/generated/), paste reports back via Ingest.
3. Rule the open decisions in `decisions.md` (Pattern A own-table etc.) — the guard keeps blocking dependent generation until each [OPEN] stub is replaced with a dated entry.
4. Mapping homework: buttons f1/f2 (stale radius, #B91C1C) await your ruling in the Prototypes panel.
5. Real-OS checks a headless rig can't do: P6-os-interop.3/4/5 (Finder drag, clipboard, drag-out), P8-sounds-wishlist.6 (queue restart under a real limit).
6. **Tailscale Serve** one-click enable (P10.1) for the remote command post — URL is in the raised todo.
7. Review digest `brain/qa/review-digest-2026-07-18.md` — ratify the autonomous calls (judgment-calls #1–59 + program-level).

## Environment notes

- App: crs-brain/ zero-dep Node :4317 (+ node-pty for the usage probe). Runs on Mac + Windows (platform-guarded spawns, .gitattributes LF/CRLF locks). node_modules gitignored → `npm install` per machine.
- Buildprint CLI: 4.1.6, Unauthorized on this Mac. Windows may already be linked.
- Per-machine gitignored state starts fresh on Windows: crs-brain/data/{state.json, notifications.jsonl, usage.json, smtp.json}, .pin, .remote-token, backups/. Digest md under brain/ is shared.
- QA rig (puppeteer-core + system Chrome) is machine-local under qa-scratch/rig (gitignored) — rebuild on Windows if browser-verifying; in-session browser panes are render-throttled, never use them for verification.
- autoCommit fires on every server data save (crs-brain/data + brain + prototypes) — expect interleaved `brain: …` commits.

## Push status

This Mac session is headless with NO GitHub credentials — `git push` fails with "could not read Username for https://github.com". Everything IS committed locally (684+ commits ahead of origin at pause). **Push from your own authenticated shell** (Mac or Windows): `git push origin main`. If the branch has diverged on origin, `git pull --rebase` first.
