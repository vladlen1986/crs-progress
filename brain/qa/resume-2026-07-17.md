# Program resume point — 2026-07-17 (Mac session end)

> Written when Vlad stopped the run to continue on the Windows PC. Read this + `program-state.md` + `master-checklist.md` first. The app runs with `node crs-brain/server.js` (run `npm install` in crs-brain/ once per machine for node-pty).

## Where the program stands

- **P1–P10 + P11.K: DONE and committed** (see `program-state.md` and git log `program:` / `qa-loop:` commits). All construction phases of the master program are complete, including the OS-grade overhaul (tokens/light mode, sweeps, multi-doc windows, editors, image viewer), notification v2, and the kanban modernization.
- **QA loop: cycle 1 COMPLETE** — 72 PASS / 5 FAIL / 5 ENV-LIMITED over 82 rows; all 5 FAILs fixed in commit `qa-loop: cycle-1` (9177b95). Checklist rows carry `2026-07-17 c1` statuses + evidence.
- **QA loop: cycle 2 INTERRUPTED mid-run** (stopped deliberately, no results recorded). **Next QA action = run cycle 2 from scratch**: full re-verify of every checklist row, both themes. Goal: two consecutive 100%-PASS runs (ENV-LIMITED rows excluded and listed). Cycle recipe: parallel static verifier + 3 serial browser verifiers (single browser), then fix FAILs serially (max 2 attempts/item/cycle, quarantine after 3 cycles).

## Standing USER OVERRIDES (bake into every future prompt; logged in brain/design/judgment-calls.md #35–#37)

1. **Kanban board = BOTTOM sheet** (40vh) in the map — never a right drawer.
2. **All toggles greyscale**: segmented controls / toggle chips active = `--bg-elevated` + `--text-primary`, never accent fill/tint. (Interpretation on record: §20.2 switches/checkboxes and active-nav states keep accent — flag to Vlad if that should change.)
3. **Single-app**: nothing opens a browser tab. Tool pages open as in-app iframe windows (`openPageWindow`), docs via `openDocWindow`. One exception: external http(s) links open the system browser with ↗.

## Next work item: Progress Tree redesign (prompt received, recon DONE, implementation NOT started)

Executive ⇄ Raw two-renderer redesign of `crs-brain/public/tree.html` per Vlad's `ptree:` prompt. Recon complete — key facts:

**Data truth:** tree = thin view over `data/modules.json` (46 modules; GET/PUT `/api/modules` whole-doc, 500ms debounce, server renumbers order, git auto-commit). Per-module detail = `GET /api/modules/detail?id=` built live by `moduleDetail()` (server.js ~1335) slicing brain/ md: techRef/userManual filename match in `brain/modules/` (documented = techRef exists → DOCS tag), `_mdSection()` extracts `## 2. Data model`, `Option Sets used`, `## 4. Permissions`, `## 5. Workflows` from the techRef + `### <name>` from `brain/STATUS.md`; `_scanMentions()` greps 5 ledger files. DoD checklist = 8 tri-state items INSIDE each module object (`{key,label,state:todo|partial|done,detail}`). Prompts: `POST /api/modules/prompt` (canned, no LLM) + `POST /api/modules/edit-prompt` (LLM, ~25-30s, client shows live ticker then auto-`sendToBp`). BP handoff contract: `localStorage['crs_bp_compose']` + `postMessage({type:'bp-compose'})` to opener, else navigate `/#bp-compose` — receiver in index.html (~3985 consumeBpCompose). No deep links exist today (only `?theme=`); free to add `#module=`.

**15 keep-capabilities (acceptance criteria — do not drop):** autosave w/ saving/ok/err indicator · reorder via drag AND buttons/menu · status cycle · tri-state checklist + focus-preserving per-item detail input · global rollup bar + counts + updated-ts · section filter + grouping + current-focus computation · lazy detail fetch/cache · all five detail content blocks · mentions ledger (file/count/6 lines/+N) · 3 file links (techRef/userManual/STATUS.md) · both prompt buttons + copy + BP-chat handoff · action logging POSTs · empty/loading/error states · esc() everywhere · DOCS badge.

**Design gaps to build:** per-row slim progress bar (derive from checklist ratio; none exists today — `.chkprog` is bare X/Y) · Executive expanded card must render the md sections as REAL tables (§20.6) — the sections come from known heading structures, parseable; anything unparseable → clean prose, never a `<pre>` fence · Raw view = the honest `<pre>`-style twin with per-block Copy (this is where today's rendering lives on).

**Sequencing:** implement ptree (single writer on tree.html) → its own micro-loop verify (both themes) → append its checks to master-checklist → THEN run full QA cycles until two consecutive greens (the ptree change resets the consecutive counter).

## Environment notes

- **This Mac**: buildprint CLI link = Unauthorized (CLI update 4.1.6→4.2.5 pending) — issue checker degrades gracefully. Windows may be linked already.
- Server-side notification/queue/watcher state lives in gitignored per-machine files (`data/state.json`, `usage.json`, `task-queue.json`, `notifications.jsonl`) — Windows starts those fresh; digest md files under `brain/bubble/` are shared and may gain one duplicate baseline section on first Windows run (known, harmless).
- Vendored libs committed: `crs-brain/public/vendor/xlsx.full.min.js` (0.18.5), `mammoth.browser.min.js` (1.8.0).
- QA loop workflow recipe (agents, slots, schemas) is described above; the previous run's script is machine-local — rebuild it from this description.
