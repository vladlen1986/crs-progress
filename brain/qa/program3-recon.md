# Brain Program 3 — Phase-0 comprehension refresh + reconciliation (2026-07-18)

As-built recon for the five sharpening phases (3 parallel agents; the chat verdict was PROVEN by executing the three-parallel-files test, not grepped). This supplements brain/design/app-map.md with what P3 specifically touches.

## Phase-1 verdict (LOAD-BEARING) — the chat ALREADY runs on Claude Code

**Proven, two ways.** (a) argv: `/api/chat` → runClaudeWithFallback (server.js:1761) → runClaudeStream (:1610) → spawnClaude (:1547) = `claude -p --output-format stream-json --include-partial-messages --verbose --permission-mode acceptEdits --allowedTools WebFetch WebSearch Bash(buildprint:*|agent-browser:*|node:*|python:*|python3:*) mcp__buildprint --settings BP_GUARD_SETTINGS --append-system-prompt <SYSTEM_PROMPT|BP_PROMPT> [--add-dir REPO_ROOT] [--model --effort] --resume <sid>|--session-id <uuid>`. No `@anthropic-ai/sdk`, no API key, no fetch to api.anthropic.com — Max subscription via CLI. (b) executed: sent a chat asking for three parallel subagents each writing a file → SSE carried three `{type:block,tool:Agent}` + three Task `detail` events (subagent_type general-purpose, run_in_background:false), and in-bounds (qa-scratch/) the three files landed on disk. A bare API wrapper cannot do that.

**Live by construction:** Task/subagents (parallel, proven; not even in allowedTools — orchestration isn't gated), resumable sessions (--resume, persisted per chat), Read/Grep/Glob + Edit/Write within cwd (acceptEdits), WebFetch/WebSearch, scoped Bash, mcp__buildprint, model-fallback chain, extended thinking, live token metering, Stop/abort. **Turn UI (f609abb = HEAD) is FULLY WIRED, not a stub** — streamChat (index.html:2463) consumes block/label/detail/think/delta/model-switch/usage; renders a collapsible "Worked for Xs" work-tree; server persists the timeline into chat.messages[].steps so it survives reload (stepsGroupEl :1946).

**allowedTools CEILING (bounds Phase 1):** Bash is prefix-restricted (no arbitrary mkdir/printf/git/rm — route through node/python/MCP); writes are cwd-boundaried (out-of-cwd needs --add-dir; bp chats already add REPO_ROOT); BP_GUARD_SETTINGS PreToolUse hook hard-blocks dangerous buildprint cmds; Skills NOT enabled (no Skill in allowedTools).

**⇒ Phase 1 is NOT a migration.** It is: (1) prove concurrency in-chat (re-run the parallel-files test as an acceptance artifact), (2) context injection at session start (engine bundle for the focus module + CLAUDE.md conventions), (3) operator slash-commands (/settle /chunk /send /todo /decide /gen) routed through existing engine/bridge/state APIs — no client slash parser exists today, sendMsg only has the "add todo" intercept, (4) finish/verify the turn UI against real subagent traffic + model-fallback notes + timestamps, (5) optionally widen allowedTools (add Skill / add-dir) — flag to Vlad, don't silently broaden the security surface.

## Phase-2 (⌘K palette) — substrate

Existing: `openPal`/`palFilter`/`palRender` (index.html:4224-4251), Cmd/Ctrl+K bound (:4251), searches state.allFiles(top10)+state.chatList(top5) client-side only, kinds file|chat, go()→smartOpen/openChat. `/api/search-chats` (server.js:2485) is the ONLY search endpoint (chats only, no index, re-reads every chat file per request). No global index. Stores to index: chats, repo docs (/api/files tree), prompts (brain/buildprint/generated/*, 14), chunk reports (prototypes/*/chunks/*-report.md), checklist rows (master-checklist.md), todos (tasks.json), modules (modules.json+moduleDetail), wishlist/ideas (feedback≈wishlist.json 11 + ideas.json empty), memory, notifications. Icons via fileIcon (icons.js:49). Commands = mutating REST endpoints (no slash parser); `>` mode maps to them. openTarget already routes surfaces vs files.

## Phase-3 (entity pages + backlinks) — substrate

moduleDetail (server.js:1968) returns module/documented/techRef/userManual/statusBlock/dataModel/optionSets/perms/workflows/mentions — ~60% of an entity page. MISSING per-module: structured decisions, generated prompts, chunks+reports, linked todos, feedback, checklist rows. Ref index is CLIENT-side in map.html (REF_DOCS = only CLAUDE.md/brain/INDEX.md/decisions.md; refEdges/refDangling; parser :554-648) — doc→doc only, no chat/report edges. Extend server-side to cover chat messages + reports. Entity page = a new served .html surface (param-driven `#id=`, like tree.html#module=) plugged into openTarget/isSurface. Auto-linking: cmClassify (index.html:1582) + cmChipEl (:1594) resolve FILE PATHS only, client-side vs DW.allPaths — extend with a server entity resolver (name→entity) feeding chip injection.

## Phase-4 (cost intelligence) — substrate

usage.json (statusline.js:35, account-wide, NOT per-run; resets_at = epoch SECONDS). runClaudeStream onUsage {in,out} computed at :1678 but persisted NOWHERE — only chat wires it (:2665) and only streams to the UI meter (ephemeral). **No usage-log.jsonl, no per-run cost record.** Every model touchpoint (chat/gen/chunk/plan/queue/memory/wishlist/digest/issue — ~11 sites) goes through runClaudeStream where token counts ARE available but unwired. routeModel (server.js:944, classifyTask :912, off unless cfg.autoRoute) is wired to EXACTLY ONE call site (wishlist prompt-gen :3052) — gen/plan/queue don't route despite being mechanical. No rate table anywhere (add Haiku/Sonnet/Opus $/token). Analytics surface = new .html surface OR inline in #dashView; chart = net-new (reuse map.html canvas pattern or inline SVG; no charting lib vendored; tree/usage .bar primitive for tiles).

## Phase-5 (hardening) — substrate

index.html = 4948 lines, ONE <script> (1516-4946, ~3430 JS lines, 373 functions, top-level lexical scope, NOT an IIFE) + 4 external scripts (icons/sounds-synth/sounds/notify-registry). Clusters: renderMd/chat-media 1532-1760, chat/streamChat 1895-2490, views/dashboard 2032-2350, FS 2735/EX 2750, DW windows 2887-3164, editors 3165-3355, surfaces 3356-3418, image viewer 3419-3535, kanban 4190-4600, settings/theme/notify 4673-4804, conn 4777, versionGuard 4617, boot IIFE 4944. ~16 shared mutable globals (state/FS/EX/DW/SETTINGS/NOTIFS/CONN/surfaceReturn + const tables) + $/svg. **SPLIT HAZARD #1: hundreds of inline onclick="fn(...)" handlers resolve against window** — a naive type=module split breaks them all unless every referenced fn is re-exposed on a window.CRS façade. Error capture: NO window.onerror/unhandledrejection (client), NO uncaughtException/error-log (server). action-log.jsonl EXISTS (server.js:69, logAction :90, via:remote stamps at 3174/3234/3533) = the audit substrate; error log is net-new (window.onerror → POST /api/action-log type:error). autoCommit (:1788) commits but does NOT push; only /api/handoff pushes (manual). No backups/ dir. Boot cold-start: 4 SERIAL awaits (loadChats→loadFiles→loadProgress→loadPlan) = the obvious parallelize win. No DOM virtualization; full innerHTML replace per render.

## Reconciliation — P3 phases vs as-built

- **P1:** verdict decided (already on Claude Code) → NOT migration; adjusted approach above. This is the biggest program-vs-reality correction and it makes Phase 1 SMALLER and safer (surface + operator layer + turn-UI-verify), not a risky backend swap.
- **P2:** extend the existing palette + build the missing server index (greenfield index; palette shell exists).
- **P3:** extend moduleDetail (partial exists) + move the ref index server-side + new entity.html surface + extend cmClassify (all have clear hooks).
- **P4:** wire onUsage → usage-log.jsonl at all touchpoints (greenfield log; hook exists) + analytics surface + routing audit (routeModel exists, under-wired — the audit's first finding).
- **P5:** module split is feasible but the inline-onclick→window coupling is the real cost (window.CRS façade approach); error capture + auto-push-backup are additive on existing substrates (action-log, autoCommit).
- **Governance:** the FULL 157-row master checklist is the regression suite (esp. for the P5 split — run before AND after). New rows: CHATV2.*, PALETTE.*, ENTITY.*, COST.*, HARDEN.*. Final loop to two greens over ALL P3 rows.

## Standing overlaps to reuse (never rebuild)

Turn UI (f609abb, wired) · openTarget/#surfaceView (surfaces) · cmClassify/cmChipEl (chip injection) · moduleDetail (entity backbone) · map.html ref parser (extend) · action-log.jsonl + via:remote (audit) · autoCommit (extend to push) · routeModel/classifyTask (routing audit) · fileIcon + notify registry (palette grouping) · the tasks/notify/dash stack (Program 2).
