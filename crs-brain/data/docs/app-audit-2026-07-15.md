# CRS Brain — App Audit (2026-07-15)

90 findings from a 6-reviewer deep analysis (UI/UX of index.html + map.html, server capabilities, planning workflow, Buildprint manual, live API walkthrough). Fixed-this-session items are marked ✅; the rest is the improvement backlog.

## HIGH (34)

- **Right-panel Progress board is dead code — planning data has no full home** _[bug-risk · ux:index.html]_
  loadProgress() (line 1386) targets $('progress'), but no element with id="progress" exists anywhere in the markup (right column, lines 619-636, contains only Usage/Recently opened/Recently edited). The function silently no-ops on every call: at boot (line 1628), after every chat stream (line 1240), and after bpTrack (line 1611). All the board CSS (.pcol/.card/.cols, lines 413-428) and the 'Missing / gaps' rendering (lines 1396-1401) are dead. Meanwhile the right expand-handle is titled 'Show usage & progress' (line 553) and the mobile ▤ button says 'Usage & progress' (line 558) — both lie. Con…
- ✅ **Dashboard Knowledge-base and 'manual pages' stats open an empty explorer by default** _[bug-risk · ux:index.html]_
  The stats at lines 1019 and 1041-1044 call openExpl('brain/bubble'), openExpl('brain/buildprint'), openExpl('brain/bubble-forum'), and openExpl('crs-brain/data/docs'). But the explorer applies the project-only filter: explNodeAt (line 1319) wraps the tree in filterTree (lines 796-800), which strips MANUAL_DIRS (bubble/buildprint/bubble-forum, line 792) from brain/ and drops crs-brain entirely (not in CRS_DIRS, line 790). With the default crsOnly=ON (line 793), all four tiles land on 'This folder is empty.' The dashboard proudly counts hundreds of manual pages, then clicking through shows nothi…
- ✅ **Chat deletion is one small click with no confirmation and no undo** _[ux-friction · ux:index.html]_
  chatItemEl (lines 803-807) puts a 13px .x delete icon directly on the row; delChat (line 856) fires DELETE immediately — no confirm(), no undo toast, no archive. The x sits inside the same click target as 'open chat', so a slightly-off click while scanning the list destroys a planning conversation permanently. Contrast: disconnecting the Claude account (a fully reversible action) DOES get a confirm() (line 1473). For a daily driver holding decision history, this is inverted risk-weighting.
- **Buildprint sync (bpTrack) lacks every resilience feature the main chat stream has** _[bug-risk · ux:index.html]_
  bpTrack (lines 1585-1614) is a hand-rolled second streaming path: no Stop button (state.abort never set, stopBtn never shown — only sendBtn.disabled=true at line 1594), no work blocks/timers, and crucially it never registers state.live. So if the user navigates away mid-sync (opens a file, dashboard, another chat), bumpView (line 665) early-returns because state.live is null, state.sending stays true, and every subsequent sendMsg silently queues (line 1060) until the sync finishes — the app feels frozen with no explanation. There is also no way to re-attach to the running sync output after nav…
- **Ideas are disconnected sticky notes — no links to files, chats, or Buildprint tasks** _[missing-feature · ux:map.html]_
  An idea is only {id, text, ts} (created at line 1175); parseIdea (lines 1018-1022) extracts just #tags and !high/!med/!low from the raw text. There is no way to attach an idea to a brain/ file, a spec, a chat thread, or a Buildprint session — the three things planning actually revolves around in this workflow. The map knows every file node (byId, line 350) and the dock already highlights files mentioned in answers (lines 1317-1323), so the plumbing for idea→file linking exists; it's just never used. Minimum viable fix: an optional `files:[]` and `module` field on the idea object, a 'link file'…
- **No module/section field — freeform #tags can invent modules, violating the locked-46 rule** _[missing-feature · ux:map.html]_
  CRS has 46 locked modules in 7 sections and CLAUDE.md forbids inventing modules, but the ideas board's only categorization is freeform #tags typed inside the text (line 1019). A typo'd or fabricated module tag looks identical to a real one. There is no module picker validated against data/CRS_Module_OptionSets.xlsx (or a brain/-derived list), no section grouping, and no per-module idea counts — so you cannot answer 'what's queued for Surveillance?' The tag filter row (lines 1133-1135) even truncates to the first 12 tags, so with 46 modules most would be unfilterable anyway.
- **Ideas cannot be edited or reordered — the 'planned' column can't express a build sequence** _[ux-friction · ux:map.html]_
  Card actions are only ◀ ▶ ✕ (lines 1159-1167). There is no edit: fixing a typo or adding a tag means delete + retype the whole idea. There is no ordering: addIdea unshifts (line 1175) and moveIdea unshifts into the target column (line 1186), so moving an idea right then left scrambles order; no drag-and-drop, no manual sort. For a planning tool the ordering of 'planned' IS the plan — what to build next in Bubble — and it's currently uncontrollable. Actions are also hover-only (CSS lines 95-96), making them flaky on touch.
- ✅ **Main loop bug: dt is always 0, freezing all rotation animation** _[bug-risk · ux:map.html]_
  Lines 887-891: `function loop(now){ last=now; ... const dt=Math.min(50,now-last); step(dt); ... }` assigns last=now BEFORE computing dt, so dt === 0 on every frame. Consequences in step() (lines 594-651): galactic rotation G (line 638) is always 0, per-file hub orbits (lines 641-644) never move, and ringAngle (line 650) never advances — so the circle/orbit/spiral/rings/sphere layouts documented as rotating (comments at 500, 516, 555, 564) are static, and the 'Ring spin' slider only affects twinkle/flow speed via ambientSpeed(), not spin. Fix: compute dt before reassigning last (const dt=Math.m…
- **Double-click-to-collapse a hub also fires single-click open twice** _[bug-risk · ux:map.html]_
  pointerup (line 956) calls openNode on every non-moved click, and openNode for a hub opens the folder explorer (lines 941-944: parent.openExpl in embed, window.open standalone). The tooltip (line 935) and hint bar (line 291) tell you to double-click a hub to collapse — but a double-click first delivers two pointerups, i.e., two explorer opens, before the dblclick handler (lines 961-963) toggles collapse. Collapsing a big cluster (the exact case collapse exists for, per line 424) always detonates the explorer. Hub clicks need either a click-delay guard or single-click reserved for select/collap…
- **The galaxy encodes nothing plan-relevant — currently eye-candy with three useful affordances** _[opportunity · ux:map.html]_
  Visual channels are spent on ornament: node size = file byte size (nSize, line 377) which has no planning meaning; planet rings = hash(path)%9 (lines 428, 445) i.e. random decoration; 9 layouts, 260-star parallax field, meteors, nebula fog and ~44 flow particles (lines 355-375, 669-801) carry zero information. The genuinely useful affordances are: search-dim (lines 998-1002), Ask-the-Brain path highlighting (1317-1323), and click-to-open (939-951). Missing overlays that would make it a planning surface: recency heat (needs mtime in /api/files — tree currently only supplies name/path/size, see …
- **Kanban stages don't match the actual Buildprint workflow; done is a dead end** _[missing-feature · ux:map.html]_
  Columns are inbox/exploring/planned/done (line 1016), which skips the pipeline the user actually runs: spec'd in brain/ → Buildprint prompt written → plan reviewed → applied on TEST → verified/ingested. 'planned' jumps straight to 'done' with no in-build state, and nothing connects a done idea to what shipped: ts is stored (line 1175) but never rendered, there's no completion date, no archive, and no feed into the weekly digest or brain/changelog.md. A 'Promote to Buildprint prompt' action on a planned idea (the app already has bp:true chat mode) is the single highest-leverage missing feature …
- ✅ **No plan storage — the plan→approve→apply→ingest loop has no artifact** _[missing-feature · server:capabilities]_
  The core workflow the app exists for has no data model. BP_PROMPT (server.js:301-320) mandates 'state the exact plan... get Vlad's go-ahead' but the plan only exists as prose inside a chat message. There is no plans store, no status lifecycle (draft → approved → applied → ingested), and no linkage between ideas.json (/api/ideas, line 1069), progress.json (/api/progress, line 1064, DEFAULT_PROGRESS line 706), a BP task chat, the resulting Bubble diff (bpSyncDiff, line 359), and the ledger update. Every stage is a separate silo, so 'which approved plans have actually been applied and ingested?' …
- **Plan-before-apply guardrail is prose-only; nothing structurally blocks apply** _[bug-risk · server:capabilities]_
  runClaudeStream passes '--permission-mode acceptEdits' (line 501) and '--allowedTools ... Bash(buildprint:*)' (line 502), which whitelists EVERY buildprint subcommand including 'apply', '--force-apply', '--no-check', and 'sync --reset'. The only thing preventing an unapproved apply is BP_PROMPT guardrails (4)-(5) (lines 309-315) — pure prompt compliance inside a single one-shot '-p' run, where the model cannot actually pause for approval anyway. A hard gate (e.g. allow only 'Bash(buildprint sync:*)', 'Bash(buildprint check:*)', 'Bash(buildprint audit:*)' by default and require an explicit per-…
- ✅ **PUT /api/settings silently wipes all settings except model/effort** _[bug-risk · server:capabilities]_
  The handler at server.js:1053-1062 builds next = { model, effort } and saveSettings(next) overwrites the whole file. This destroys manualsCheckedAt (so the weekly manual scrape re-runs within an hour of any settings save — see gate at line 1178) and destroys any bpAutoTrack:false the user hand-set, silently re-enabling the 20-min auto-track (check at line 1205). Fix: spread current settings first ({ ...loadSettings(), model, effort }).
- ✅ **findBpWorkspace can silently select a non-test (even live) branch clone** _[bug-risk · server:capabilities]_
  findBpWorkspace (server.js:274-292) prefers 'test'/'dev' but then falls back to the FIRST branch subdirectory found (lines 284-288) — if a 'live' or 'main' clone exists without test/dev, the copilot runs there while BP_PROMPT still claims 'TEST branch only — NEVER live' (lines 309-310). /api/bp/status (line 936) returns the branch name but the UI conn line (index.html:1578) shows no warning when branch !== 'test'. The fallback contradicts the project's hardest guardrail; it should refuse or require explicit confirmation.
- ✅ **No Buildprint prompt/task template support anywhere server-side** _[missing-feature · server:capabilities]_
  CLAUDE.md's locked workflow says 'Claude writes the Buildprint prompt; user pastes into Buildprint', and recurring task shapes exist (Pattern A field+privacy-rule addition, option-set sync, security audit), but there is no template storage, no 'generate a BP task prompt from this idea/progress item' endpoint, and no reuse mechanism. The only prompts offered are 3 hardcoded examples in bpHeroHtml (index.html:837). A small templates.json + an endpoint that expands a template with entity names (grounded in brain/database.md) would directly accelerate the plan→prompt step of the loop.
- **bpSyncDiff ignores sync failure — ledger goes silently stale** _[bug-risk · server:capabilities]_
  bpSyncDiff (server.js:359-374) does 'await runBuildprint(["sync"], ws)' at line 362 and never checks the result. If sync fails (Buildprint auth expired, network down), the git diff runs against the old snapshot and reports 'no changes'; auto-track (maybeTrackBuildprint line 1211) then does nothing forever with only a console.log on exceptions. The user believes the brain tracks Bubble while it silently doesn't. The sync result should be checked and surfaced via /api/bp/status and a UI badge.
- ✅ **Manual bp/track advances the diff baseline even when the ingest was aborted** _[bug-risk · server:capabilities]_
  In POST /api/bp/track (server.js:943-978), after runClaudeStream returns, line 973 writes fs.writeFileSync(BP_TRACK_FILE, r.head) without checking result.aborted. If the user closes the tab mid-ingest (req 'close' → ac.abort(), line 948), the claude run is killed with a partial ledger update, yet the baseline still advances — those Bubble changes are permanently skipped by all future syncs. Compare the auto path (line 1220) which at least only advances after a non-thrown run. Guard: only advance baseline when !result.aborted.
- ✅ **Missing artifact: a persistent per-module 'build packet' between idea and Buildprint prompt** _[missing-feature · planning:knowledge]_
  Today the chain is: module list (data/CRS_Module_OptionSets.xlsx, 46 locked) -> field-level spec (specs/CRS_Blueprint.html, 68 DTs) -> ad-hoc chat -> one-off Buildprint prompt. The only surviving 'plan' is prose buried in decisions.md (e.g. 2026-05-02 entry lines 60-97: Casino Settings card-by-card status + 'Next session priority'). Nothing in the repo holds an ordered, checkable list of Buildprint-sized steps per module with per-step status. The Blueprint is a spec (what it should be), brain/ is a ledger (what exists); the missing middle layer is the plan (what to do next, in what order, and …
- **Product build has been stalled ~10 weeks while the tool grew; the day-to-day need is a visible resume point** _[ux-friction · planning:knowledge]_
  The newest product-build entry in decisions.md is 2026-05-02 (Casino Settings RE half-done, lines 60-97); today is 2026-07-15. brain/changelog.md has exactly one entry — the 2026-07-14 init — meaning zero Buildprint session ingests have ever happened. All momentum since May went into crs-brain/ itself (PROGRESS.md sections 2-3 are entirely tool work). What Vlad's daily planning workflow actually needs is not more chat UX but a single answer to 'what is the next Buildprint prompt?' surfaced on the dashboard: active module, current step in its build packet, and a one-click 'draft the prompt' act…
- **Prerequisite before any prompt creator: run and ingest the Buildprint Plan-mode inventory** _[bug-risk · planning:knowledge]_
  brain/database.md line 16 says everything except Company/Property/User is 'spec-only until confirmed by a Buildprint inventory ingest', and its Pending list (lines 73-76) puts the Plan-mode inventory of actual DTs/Option Sets/privacy rules/legacy workflows as first priority — same in brain/changelog.md line 9. Task 30 prompts generated against unverified state will collide with the legacy Bubble app (Report DT already has 50+ fields and 22 soft-deleted fields per brain/database.md line 20). The inventory ingest is a one-session task with existing plumbing (bp:true chat + Ingest to Brain button…
- ✅ **Task 30 (Buildprint prompt creator) is the right centerpiece, but it should read/write the build packet, not be a one-shot generator** _[opportunity · planning:knowledge]_
  PROGRESS.md line 35 scopes Task 30 as 'reads progress + ideas + design/tokens.css + full app plan -> structured plan -> precise Buildprint prompt'. The high-value version: the 'structured plan' it produces is persisted as the per-module build packet file (finding 1), each generated prompt corresponds to exactly one step of that packet, and after the Buildprint session the existing bpSyncDiff/track + ingest flow flips that step's status. That turns Task 30 into the state machine that closes the loop plan -> prompt -> apply -> track -> ingest -> next step, instead of a stateless prompt template.…
- **Per-module build packet structure that satisfies the locked architecture** _[missing-feature · planning:knowledge]_
  Template sections, each mapped to a locked rule: (1) Header — module name+key exactly as in data/CRS_Module_OptionSets.xlsx (CLAUDE.md line 39: do not invent modules), section (1 of 7), page_route for the App-page v param (CLAUDE.md line 33, sidebar pattern line 172), links to Blueprint anchor / decisions.md entries / brain files. (2) MVP scope — in/out, 'MVP first' rule line 130. (3) Data Types — per DT: new-or-existing, field table with Bubble types, and a mandatory tenancy row confirming company + property fields present or the DT is a decisions.md-listed exception (lines 131-132). (4) Opti…
- **Capabilities: Buildprint edits app STRUCTURE only — schema, option sets, workflows, pages, styles — never database Things** _[opportunity · buildprint:manual]_
  Build mode / CLI can create and edit: pages, mobile views, elements, reusable elements, workflows, backend workflows, data types, option sets, styles, settings, and supported API Connector configuration (permissions-and-data-safety.md:68, how-buildprint-works.md:12). Database access is strictly read-only (search/fetch/aggregate) and exists only for investigation — 'Build mode cannot edit Bubble database Things' (permissions-and-data-safety.md:68, how-buildprint-works.md:17, crs-brain-operations.md:42). It also cannot see private API Connector values or plugin secrets (filtered from snapshots),…
- **Good prompt anatomy: 7 required slots per Buildprint's own best-practices doc** _[opportunity · buildprint:manual]_
  best-practices-for-using-agents.md:12-24 lists what a prompt must define: (1) the branch, (2) the exact page/reusable/workflow/data type involved, (3) expected behavior, (4) the current problem, (5) constraints and edge cases, (6) plan-first vs build, (7) clear success criteria. Reference example at line 24: 'On the test branch, update the signup page so users see a clearer error when their invite code is invalid. Inspect the current workflow first, then explain your plan before editing.' Also: be explicit about what NOT to change (lines 81-89, e.g. 'Do not change the payment workflow, Stripe …
- **Good prompt discipline: one bounded task per chat, Plan mode gate for risky work, restart-fresh pattern** _[opportunity · buildprint:manual]_
  best-practices-for-using-agents.md:28-38: never combine unrelated work in one chat; split into investigate → propose → change → test → review. Lines 42-46: use Plan mode when the task is large/unclear/risky; switch to Build only after the plan is 'specific enough to review'. Lines 50-60: when an agent goes in circles, don't pile on corrections — restart a new chat with goal summary, branch, what was tried, what to ignore, exact next step. Lines 68-79: changes touching privacy, permissions, data structure, backend workflows, payments, or API Connector calls warrant review. The generator should …
- **Guardrail: CLI has NO programmatic branch restriction — TEST-only is enforced by convention alone** _[bug-risk · buildprint:manual]_
  permissions-and-data-safety.md:80: 'this will not programatically restrict to certain branches / projects. When you use the CLI, it is your responsibility to ensure changes are applied to the right place.' Combined with crs-brain-operations.md hard rule 1 ('dev/test branch ONLY. Never clone, edit, or apply the live branch'), nothing technical stops an apply to live. Any in-app prompt generator or command runner MUST hard-code the branch: name the test branch explicitly in every generated prompt/command, verify cwd is the ~/projects/crs-bubble/casinoreportingsystem/test worktree before executin…
- **Guardrail: generator must bake in the sync → edit → check → apply loop and the forbidden-flag list** _[bug-risk · buildprint:manual]_
  crs-brain-operations.md:16-21 and rules 2-3: `buildprint sync` ALWAYS first (editor edits invisible until synced; stale apply can clobber them — cli-applying-changes.md:31,70-76), `buildprint check` must pass, then `buildprint apply`. Forbidden without Vlad's explicit same-conversation approval: `--force-apply`, `--no-check`, `sync --reset --confirm` (crs-brain-operations.md rule 3). The docs add two more danger flags the generator should also block by default: `--allow-suspicious-shrink` (cli-applying-changes.md:81, guards against bad exports) and `--allow-large-apply` (cli-applying-changes.m…
- **Guardrail: plan-before-first-apply and post-apply brain ingest are session rituals the app should template** _[missing-feature · buildprint:manual]_
  crs-brain-operations.md rules 4-5: before the first apply of a session, describe exact files/entities to change and expected Bubble effect and get Vlad's go-ahead; after every applied change, report what changed and ingest the summary into brain/ (database.md / workflows.md etc.) in the same session. Rule 7: stop and surface anomalies (stale branch, suspicious shrink, conflicts), never bulldoze. Apply already emits a JSON result line (app, branch, elapsed, change count — cli-applying-changes.md:37) and check supports --json (cli-validating-with-check.md:59), so the app's existing bp auto-track…
- **Guardrail: Pattern A schema invariant must be auto-appended to every schema prompt** _[bug-risk · buildprint:manual]_
  crs-brain-operations.md rule 6 + CLAUDE.md locked decision 1: every business Data Type gets `company` + `property` fields and privacy rules checking BOTH (Current User's company = This Thing's company AND property = property). The CLI scaffold `buildprint new data_type` (cli-creating-and-copying-entities.md:36-44) does NOT add these automatically — the generator must inject `--field company:company --field property:property`-style fields and an explicit instruction to create both-field privacy rules into any prompt that creates or modifies a data type, plus flag known exceptions (Company, Subs…
- **/api/progress is stale and contradicts the brain ledger** _[inconsistency · ux:live-walkthrough]_
  GET /api/progress returns updated:'2026-05-02' — 10+ weeks before today (2026-07-15) — with exactly 1 item in 'now' (Casino Settings RE), an empty 'next' column, and an empty 'missing' array. Meanwhile the brain ledger shows major recent work the board ignores: brain/security.md (mtime 2026-07-13) records 24 data types with no privacy rules and a pending Pattern A rollout, and chat 53175902 (2026-07-14) produced an 8-step Pattern A migration plan. None of this reached the board. The board is the dashboard's central planning surface, and its data is effectively dead — nothing (bp/track ingestio…
- ✅ **No plan storage — build plans live only as chat transcripts** _[missing-feature · ux:live-walkthrough]_
  The core workflow is plan-before-apply, yet a generated plan (e.g. the '8-step migration plan for adding Pattern A', chat 53175902) exists only inside a chat message blob. There is no /api/plans endpoint, no persisted plan object with steps/status, no way to mark step 3 of 8 done, and no way to hand a stored plan step to the Buildprint chat (/api/bp/chat) for execution. The full route list in server.js (lines 753-1153) confirms nothing plan-shaped exists. This is the single biggest data-model gap for planning CRS module builds.
- **No CRS module/section model or endpoint** _[missing-feature · ux:live-walkthrough]_
  Chats reference '7 CRS sections' and data/CRS_Module_OptionSets.xlsx exists, but no endpoint exposes a structured module list with per-module status (data types built, option sets seeded, privacy rules applied, workflows pending). Progress items are bare {id,title,note} strings (DEFAULT_PROGRESS, server.js lines 706-715) with no module linkage, no dates, no effort, and no links to brain files, specs/, or Bubble entities. A planning dashboard for a multi-module SaaS cannot answer 'which module is next and how complete is it' from this data model.
- **/api/bp/status returns nothing actionable for planning** _[missing-feature · ux:live-walkthrough]_
  GET /api/bp/status returns only {ready:true, app:'casinoreportingsystem', branch:'test', baseline:true} (server.js lines 936-940: findBpWorkspace() plus an existsSync check on BP_TRACK_FILE). It omits: when the last sync happened, whether Bubble has drifted since baseline, how many changed files are pending ingestion, and the last savepoint/applied change. The diff logic exists (bpSyncDiff computes base..head changed-file lists) but is only invoked inside POST /api/bp/track, so the dashboard cannot show 'N Bubble changes pending' without running a full sync-and-ingest.

## MEDIUM (40)

- ✅ **Buildprint 'connected' dot is invisible — var(--success) is undefined** _[bug-risk · ux:index.html]_
  Lines 323 and 342 use var(--success) for the connected-state dot, but :root (lines 10-17) defines --good, not --success. .bp-conn .bp-ok therefore renders with transparent background — when the Buildprint CLI IS connected, bpStatus (line 1580) shows text with an invisible dot, indistinguishable at a glance from the muted 'not connected' dot. One-token fix: --good. (Line 323 is in a dead CSS block anyway, see separate finding.)
- **Queued prompts can strand or land in the wrong chat** _[bug-risk · ux:index.html]_
  A queued job (sendMsg, lines 1058-1060) captures only text/attachments/ingest — not the chatId or bp mode it was typed in. Execution uses state.chatId and state.bp at run time (runJob line 1063 → streamChat body, line 1204). Two failure modes: (1) switch chats while a reply streams → when the now-orphaned stream finishes, mine() is false (line 1239) so dequeue() never runs — queued prompts sit in the strip forever until some later send completes in-view; (2) when they finally dequeue, they post into whatever chat/mode is currently open, e.g. a planning question queued in a normal chat can fire…
- **Returning to a still-generating chat hides its earlier history** _[bug-risk · ux:index.html]_
  The re-attach path in openChat (lines 845-851) does log.innerHTML='' and appends only the live user message + streaming reply node — every earlier message in that conversation vanishes. After the stream completes, nothing re-fetches the transcript (the finally block at line 1240 only calls loadChats/loadProgress), so the user must manually click the chat again to see full history. Re-attach should render the persisted messages first (api('/api/chat?id=')) and then append the live node.
- ✅ **Clicking the 'Chats' tab discards your place and opens a blank conversation** _[ux-friction · ux:index.html]_
  setTab (line 783): if chatView is hidden (you were reading a file, the dashboard, or the map), clicking the Chats tab calls newChat() — wiping state.chatId and landing on the empty hero. The natural expectation for a daily user is 'take me back to the conversation I was in.' Should restore state.chatId via openChat when one exists, and only fall back to newChat when there is no current chat.
- **Weekly digest dumps its raw prompt and JSON payload into the chat as your message** _[ux-friction · ux:index.html]_
  runDigest (lines 1245-1257) builds a multi-paragraph instruction plus the full progress board as a fenced JSON block, then streamChat renders that whole string as the user bubble (line 1129). Every digest chat starts with a wall of machine text attributed to you, and it pollutes chat search results too. The context should travel out-of-band (a server-side digest endpoint or a hidden context field), with the visible user message being just 'Weekly digest'.
- ✅ **Reopened chats have no 'Try again' / 'Continue' controls** _[missing-feature · ux:index.html]_
  addRetryBar is only attached during a live stream's finalize (line 1198). renderChat (lines 870-881) adds only Open-as-doc/Copy actions. So after reloading the page or reopening any conversation, the last reply can't be regenerated and a stopped reply can't be continued — the affordance evaporates on navigation. renderChat should call addRetryBar on the final assistant message.
- ✅ **File tree and 'Recently edited' go stale after Ingest / brain-writing chats** _[ux-friction · ux:index.html]_
  An Ingest run (or any chat where the CLI writes brain/ files) ends in streamChat's finally (line 1240), which refreshes chats and the (dead) progress board but not loadFiles() or loadRecentEdited(). New/renamed brain files don't appear in the tree, Cmd+K (state.allFiles, line 1265), or the right rail until a manual reload. Since the whole point of Ingest is 'facts got routed into files,' the file surfaces should refresh right after — cheap to add to the same finally block, at least when opts.ingest or bp is true.
- **HTML files (including the Blueprint spec) can't be read in-app — except via map deep links, which show raw source** _[inconsistency · ux:index.html]_
  Tree click (line 1280), recent files (line 1291), explorer (line 1325) and smartOpen (line 721) all route .html to apiOpen (OS browser), so specs/CRS_Blueprint.html — a primary planning document — is never readable inside the app. Yet openFromHash '#file=' (lines 1618-1621, used by the galaxy map) calls openFile directly, which renders HTML as escaped <pre> source with an Edit toggle (regex at line 1362 even allows editing html). Two contradictory behaviors for the same file type. Consider an in-app iframe preview for HTML (like the map iframe) with 'Open in browser' as the secondary action.
- **Chat list shows message counts but no recency — hard to navigate a daily archive** _[missing-feature · ux:index.html]_
  chatItemEl (lines 803-808) renders title + count badge only. There are no timestamps, no Today/Yesterday/This-week grouping, and no pinning. For someone opening this every day to resume planning threads, 'which of these 30 chats was yesterday's schema discussion?' requires opening them. The digest chats (titled with dates) partially compensate, which suggests the need is real. The data exists server-side (chats have order); surface an 'ago' label like the recent-files rail already does (agoTxt, line 1287).
- **Plan (Now/Next/Later) is read-only everywhere in the UI** _[opportunity · ux:index.html]_
  Dashboard progress rows are explicitly inert ('drow static', line 1007; cursor:default line 307). With the right-panel board dead, there is no surface to add, reorder, or complete Now/Next/Later items short of hand-editing the source file the server parses. For an app whose stated purpose is helping PLAN the CRS build, the plan itself is the least interactive object in the UI — compare the ideas board (in the map iframe) which is editable. Even a minimal 'mark done / promote Next→Now' on the dashboard card, or a chat shortcut that edits the plan file, would close the loop.
- **Buildprint status is fetched once at boot and buried in the tab** _[bug-risk · ux:index.html]_
  bpStatus (lines 1576-1583) runs only in the boot IIFE (line 1628) — no interval (unlike usage, polled every 30s at line 1629) and no re-check when switching to the Buildprint tab or starting a bp task. Connect the CLI after loading the page and the app claims 'not connected' until a full reload. Also #bpConn only exists inside bpActions, so from a Buildprint conversation itself you can't see connection/branch state. Cheap fix: call bpStatus() from setTab('buildprint') and newBpChat().
- **Ask-the-Brain dock is a second, weaker chat client that pollutes shared history** _[inconsistency · ux:map.html]_
  Standalone-only (hidden in embed at line 1331). It renders answers via textContent (line 1310) — no markdown, so brain answers full of headings/lists/code render as raw text; it has no history browsing, no bp:true Buildprint mode, and dockChatId is lost on refresh (line 1291). Yet every question silently appends a path-mention instruction (line 1298) and writes a chat titled 'Map — …' into the shared history (line 1302), cluttering the app's chat list with oddly-formatted threads. Meanwhile its one unique feature — highlighting mentioned files on the galaxy (1317-1323) — is unavailable from th…
- **Node-size slider silently destroys the size encoding** _[bug-risk · ux:map.html]_
  The slider handler (line 994) recomputes r from n.bytes, but file nodes are created without a bytes property — addNode calls at lines 427-428 and 444-445 pass only r:nSize(f.size). So the first touch of the slider collapses every file to nSize(600) uniform size. Fix: store bytes:f.size on the node.
- **Ideas persistence is cache-forever + last-write-wins whole-document PUT** _[bug-risk · ux:map.html]_
  loadIdeas caches on first fetch and never refetches (line 1037: if(!ideas)); saveIdeas PUTs the entire document fire-and-forget with no error handling or response check (line 1038). With the map open both embedded and standalone (or alongside the app's own ideas surface), the stale copy clobbers the fresh one on the next add/move/delete. The footer claims 'git-versioned, syncs with your repo' (line 1182), but the client has no refresh, no conflict detection, and swallows save failures — an idea can be silently lost.
- **Relative links in rendered markdown 404 — breaks brain/INDEX.md hub navigation** _[ux-friction · ux:map.html]_
  miniInline (line 1199) emits `<a href="$2" target="_blank">` verbatim. brain/INDEX.md — which CLAUDE.md designates as the entry point that 'maps every domain to its file' — is built of relative links like brain/schema.md; clicking one opens http://host/brain/schema.md in a new tab, which the server doesn't serve. The preview should intercept relative .md links and call openPreview on the target path, which would turn the preview panel into an actual brain browser.
- **File-type filter chips are hardcoded and single-select; no folder or recency filters** _[ux-friction · ux:map.html]_
  Line 1077 hardcodes ['md','html','css','js','py','json','txt']: '.htm' files escape the 'html' chip (filter is endsWith('.'+ext), line 1082), and .xlsx — including the authoritative data/CRS_Module_OptionSets.xlsx — has no chip at all. Extensions should be derived from the actual tree. All filter chips (ext, tag, priority) are single-select toggles, and there is no 'recently changed' filter, which is the filter a planner reaches for first.
- **ARMS layers are hardcoded literals that will silently drift stale, and leak into CRS-only mode** _[inconsistency · ux:map.html]_
  APPS/ROUTINES/SKILLS (lines 341-347) are static arrays despite the comment 'the agentic layers (real, from this app + workflow)' (line 340) — adding a routine or skill to the actual app never updates the map. Also, CRS-only mode is documented as 'product knowledge only — hide the tool internals' (line 321) but contentTree() (lines 325-331) filters only the file tree; buildGraph unconditionally adds all ARMS nodes (lines 391-397), so tool internals remain on screen in CRS-only mode.
- **No mutual exclusion between manual bp/track, auto-track, and BP chats** _[bug-risk · server:capabilities]_
  The bpTracking flag (server.js:1201, checked at 1203) only guards the auto job against itself. POST /api/bp/track (line 943) and POST /api/chat with bp:true (line 899) can run concurrently with maybeTrackBuildprint: two 'buildprint sync' invocations racing in the same worktree, two claude processes editing the same brain/*.md files, double-ingesting the same base..head diff (duplicated changelog.md entries), racing writes to BP_TRACK_FILE, and interleaved autoCommit (line 609) git add/commit. A single shared 'bpBusy' lock returned by /api/bp/status would fix it cheaply.
- ✅ **Auto-track truncates diffs to 150 files with no 'and N more' marker** _[inconsistency · server:capabilities]_
  Manual track builds the message with a remainder note (server.js:956-957: 'more' suffix). The auto path (maybeTrackBuildprint lines 1213-1214) does listShown = r.changed.slice(0, 150) and never appends the remainder, so on a large Bubble change (e.g. first big schema pass, or a Buildprint agent run touching many pages) the model is told a silently incomplete file list and BP_TRACK_PROMPT's 'Reflect ONLY real changes shown in the diff' guarantees entities are missed from the ledger with no trace.
- **BP_TRACK_PROMPT never updates progress.json or routes decisions — unlike INGEST_PROMPT** _[inconsistency · server:capabilities]_
  INGEST_PROMPT (server.js:259-270) has step 3 (flag architectural decisions to decisions.md, never write them into brain/) and step 5 (update crs-brain/data/progress.json). BP_TRACK_PROMPT (lines 326-338) has neither: Bubble-built items move Pending→Current in brain/ files but never move on the progress board, and a decision-shaped change (e.g. a new DT that alters architecture) has no routing rule. Since auto-track is the highest-frequency ingest path (every 20 min), the progress board — the thing the digest and 'what's next' answers are grounded in — drifts from reality.
- **No Buildprint status surfacing: sync time, audit, check/apply outcomes invisible** _[missing-feature · server:capabilities]_
  GET /api/bp/status (server.js:936-940) returns only {ready, app, branch, baseline}. runBuildprint (line 340) can run any subcommand, but no endpoint exposes 'buildprint sync status', 'buildprint audit', 'buildprint changelog', last sync timestamp, last auto-track run/result, or the check/apply outcome of the last task — the UI conn line (index.html:1578) can only say connected/not. For a plan→run→ingest loop the user needs at minimum: last synced at, last apply result, pending un-ingested diff size, auto-track health. All the primitives exist server-side; it's one aggregation endpoint away.
- **Digest has no cadence — 'Weekly digest' is a button, never scheduled** _[missing-feature · server:capabilities]_
  runDigest lives client-side (index.html:1245-1258) behind a button (line 533); nothing runs weekly. The server already has the timer pattern (maybeSyncManuals lines 1195-1196, maybeTrackBuildprint lines 1226-1227) and settings persistence — a lastDigestAt gate that auto-generates the digest chat (POST /api/chat already accepts a forced title, line 837-838) or at least surfaces a 'digest overdue' flag would make the weekly rhythm real. Also /api/digest-context supports days=1-90 (line 1148) but the UI hardcodes 7.
- **Digest grounding is weak: autocommit-noise git log + progress.json only** _[ux-friction · server:capabilities]_
  GET /api/digest-context (server.js:1147-1151) returns just gitLog(days) and progress.json. The repo's git history is dominated by autoCommit entries 'brain: <chat title truncated to 60 chars>' (autoCommit lines 609-618) — one per chat exchange, so the digest mostly summarizes chat titles, not build progress. It omits the highest-signal sources it already owns: brain/changelog.md (every ingest appends a dated entry per INGEST_PROMPT/BP_TRACK_PROMPT), ideas.json movement, and Buildprint sync history (which Bubble entities changed this week). The digest prompt (index.html:1252-1255) then forbids …
- **Auto-track runs are invisible and untogglable in the UI** _[ux-friction · server:capabilities]_
  maybeTrackBuildprint (server.js:1202-1227) spends Claude turns every 20 min when Bubble changed, saving a chat titled 'Buildprint sync (auto) — …' (line 1215) and console.logging — but there is no toast/badge in the UI, no endpoint reporting the last auto-track time/result, and no settings UI for bpAutoTrack (only honored at line 1205, and any settings save wipes it per the PUT /api/settings bug). The user can't tell whether the ledger is being kept current or whether turns are being consumed.
- ✅ **On a claude error, the user's message (and new chats entirely) are lost** _[bug-risk · server:capabilities]_
  In POST /api/chat, the user turn is pushed at server.js:860 but saveChat only runs in the success path (line 920). The catch block (lines 923-925) sends an SSE error and returns without saving, so a claude failure (CLI exit, 10-min idle kill at line 594) drops the typed message; for a brand-new chat nothing is persisted at all. Persisting the chat before streaming (or in the catch) preserves the turn for retry via the existing regenerate path (lines 841-847).
- **Main-chat SYSTEM_PROMPT promises Buildprint CLI access it can't properly deliver** _[inconsistency · server:capabilities]_
  SYSTEM_PROMPT (server.js:237-242) tells the brain 'you can run the Buildprint CLI directly via Bash (buildprint …)', but normal chats run with cwd REPO_ROOT (line 511) and no --add-dir for ~/projects/crs-bubble (addDirs only set for bp chats, line 904) — so Read/Edit tools can't touch the workspace and buildprint commands execute from the wrong directory. Either route such requests to BP mode explicitly in the prompt ('tell the user to start a Buildprint task') or always pass the workspace via --add-dir and state the correct cwd.
- **Galaxy map/kanban visualize repo files only — the Bubble app itself is unmapped** _[opportunity · server:capabilities]_
  map.html reads /api/files which walks REPO_ROOT (server.js:753-754, walk at line 406); the cloned workspace at ~/projects/crs-bubble (data_types/, pages/, option_sets/, styles/) is outside the repo and invisible. For PLANNING the CRS build, a Bubble-entity view would be far higher leverage: which of the 46 modules have DTs, which DTs are missing company/property fields or a Pattern A privacy rule (statically checkable from data_types/*), option-set coverage. The findBpWorkspace + runBuildprint primitives already exist; one read-only endpoint walking ws.dir would feed it.
- ✅ **Backlog priority within PROGRESS.md section 4: Task 30 then Task 29; Tasks 27/28/33 are low planning value** _[opportunity · planning:knowledge]_
  Task 29 ('meaningful dashboard', PROGRESS.md line 36) is the natural second priority: repoint the dashboard from tool stats (files, conversations, manual pages) to product-build state — modules by build stage read from the plan files, DTs built vs spec-only from brain/database.md, next step of the active packet, days since last ingest. Task 33 (attachments bar), Task 27 (pinned messages) and Task 28 (chat folders) are chat conveniences that do not move the plan->prompt gap at all; the current ordering (33 first, line 34) optimizes the wrong thing for the stated goal of shipping CRS.
- ✅ **Doc drift: CLAUDE.md still describes the pre-CLI 'paste into Buildprint' workflow and 'DEV branch' naming** _[inconsistency · planning:knowledge]_
  CLAUDE.md lines 103-118 (added 2026-05-01) say 'Claude writes the Buildprint prompt; user pastes into Buildprint' and 'Build mode on DEV branch', while PROGRESS.md lines 21 and 49 describe the current reality: Buildprint operated in-app via bp:true chat against the TEST branch clone at ~/projects/crs-bubble/casinoreportingsystem/test, with sync->check->apply and plan-before-first-apply guardrails. Task 30 must target the in-app flow, and CLAUDE.md should be updated so future sessions (and the prompt creator's own system prompt) don't regenerate the obsolete paste workflow or the wrong branch n…
- **Capabilities: CLI has typed scaffolds (buildprint new / copy) the generator can target instead of free-text asks** _[opportunity · buildprint:manual]_
  cli-creating-and-copying-entities.md documents exact scaffold syntax: `buildprint new data_type --name X --field display:type` (fields as title:text, owner:user, status:option.project_status), `new option_set --name X --value "Display:db_value" --attribute color:text`, `new workflow --path api --name X --type CustomEvent|APIEvent|ButtonClicked|ConditionTrue --param name:type --returnParam n:t --actions N`, `new action --path <wf> --type ChangeThing --after/--before`, `new page/reusable --element-type Popup`, plus `buildprint copy root|workflow|element|action` (mints fresh ids). Scaffolds write…
- **Friction: check is changed-files-only — no whole-app scan — so audit must be a separate routine step** _[ux-friction · buildprint:manual]_
  cli-validating-with-check.md:20-24: 'a pre-existing Bubble violation on a page you have not touched will not be surfaced… There is no flag to force a whole-app scan.' Passing check therefore proves nothing about untouched pages. crs-brain-operations.md:25-26 already designates `buildprint audit` (6-check security scan: public DTs without privacy rules, public backend workflows, exposed uploaders, temp-password leaks, missing redirects, secrets) as the routine pre-session check. The app could run/prompt `buildprint audit` + `buildprint sync status` at bp-session start and surface results, closi…
- **Friction: sync merge conflicts drop a non-engineer into raw git** _[ux-friction · buildprint:manual]_
  cli-applying-changes.md:76: on conflicts, sync 'tells you to resolve them with git status / edit / git add / git commit, or to run buildprint sync --reset --confirm to discard local work'. For Vlad (non-engineer) this is the scariest failure mode — and the escape hatch (--reset --confirm) is on the forbidden list. The app could detect the conflict state, explain it in plain language, and hand the resolution to the headless claude session with the stop-and-surface rule, rather than leaving Vlad staring at git output.
- **Friction: check failures write schema-search hint artifacts the app should auto-surface** _[missing-feature · buildprint:manual]_
  cli-validating-with-check.md:49: when a check fails with expression errors it writes schema-search hints into `.buildprint/outputs/` and prints the path. Today that artifact is easy to miss in terminal scroll. The app's bp mode could watch for check failures, read the hints file automatically, and feed it back into the next prompt iteration — turning a cryptic failure into a self-correcting loop.
- **Friction: changelog + savepoints are underused review surfaces** _[opportunity · buildprint:manual]_
  cli-applying-changes.md:94-115: `buildprint changelog <a> <b>` produces a readable Markdown/JSON diff between branch workspaces (output path must be outside both workspaces). crs-brain-operations.md:26 notes savepoints via git history. The app could auto-generate a changelog before/after each bp session and pipe it straight into the existing Ingest-to-Brain flow — giving the plan-review step (rule 4) a concrete artifact and the digest a real change record instead of model-remembered summaries.
- **Inconsistency: two documented Buildprint workflows coexist — web-chat paste (CLAUDE.md 2026-05-01) vs direct CLI (crs-brain-operations.md 2026-07-14)** _[inconsistency · buildprint:manual]_
  CLAUDE.md 'Bubble work via Buildprint' still says 'Claude writes the Buildprint prompt; user pastes into Buildprint' on DEV branch, while crs-brain-operations.md:40-41 says the CLI 'replaces pasting Claude-written prompts into the Buildprint web chat for routine schema/workflow edits' and complements web agents only for big exploratory work. A prompt generator must know which target it is generating for: CLI command sequences (routine schema/OS/workflow edits) vs web-chat Build/Plan prompts (large exploratory tasks). Building both output modes — with the mode chosen by task size/risk — resolve…
- **Opportunity: MCP integration is set up-pending and would give every session live data/logs/WU context** _[opportunity · buildprint:manual]_
  crs-brain-operations.md:12 lists the optional MCP step as unchecked: Buildprint → Integrations → MCP → create server, then `claude mcp add --transport http --header "Authorization: Bearer <TOKEN>" buildprint <ENDPOINT>` — adding read-only data search, logs, WU usage, automations, and tests to every Claude session. For a PLANNING tool this is high leverage: grounded answers about actual record counts, log errors, and WU costs instead of snapshot-only structure. External MCP access is read-only unless edits are explicitly enabled and can be branch-restricted (permissions-and-data-safety.md:70-76…
- **Ideas board is completely empty — the feature has no feeder** _[ux-friction · ux:live-walkthrough]_
  GET /api/ideas returns all four columns (inbox/exploring/planned/done) empty. Nothing pipes into it: chat insights, weekly-digest suggestions, and audit findings (e.g. the 24 missing-privacy-rule data types in brain/security.md) never land in inbox. The board only changes via manual PUT of the whole document. As data for a planning dashboard it currently contributes zero signal.
- **/api/files is 89% reference documentation with no way to tell it apart** _[ux-friction · ux:live-walkthrough]_
  The tree contains 844 files; 673 are brain/bubble/** (mirrored Bubble manual) and 74 are brain/buildprint/** (Buildprint docs) — 747 reference docs vs only 8 actual CRS ledger files (brain/changelog.md, database.md, design.md, INDEX.md, migrations.md, option-sets.md, security.md, workflows.md). GET /api/files (server.js line 753) walks REPO_ROOT and returns everything with no 'reference vs project' flag, so the galaxy map/kanban mostly visualizes documentation and every client must hardcode path filters (as /api/recent-edited already does with its own PROJ set at line 760).
- **Weekly digest context is dominated by CRS Brain app commits, not CRS progress** _[inconsistency · ux:live-walkthrough]_
  GET /api/digest-context returns the raw git log of the whole crs-progress repo. Of the ~30 most recent commits, the majority are CRS Brain UI work ('Sidebars collapse to icon rails', 'Fix usage panel', 'Embed the galaxy map in-app') plus low-signal 'brain: <first chat message>' auto-commits (including duplicated typo titles). Actual CRS Bubble progress (security audit ingest, option-set work) is buried. A digest built from this tells Vlad what happened to his tool, not to his product.
- **/api/recent-edited gives paths and mtimes only — no 'what changed'** _[missing-feature · ux:live-walkthrough]_
  The endpoint returns [{path, mtime}] for 8 brain files. There is no change summary, no linked commit, and no diff reference, even though every write is auto-committed (autoCommit calls exist for ideas, bp-sync, chats). The dashboard can show 'security.md changed 2026-07-13' but cannot answer 'what changed in CRS security this week' without opening the whole file in chat.

## LOW (16)

- **Dead Buildprint copilot panel CSS + non-clickable hero suggestions** _[inconsistency · ux:index.html]_
  Lines 315-343 style a right-panel Buildprint copilot (.rtabs, .bp-panel, .bp-head, #bpWs, .bp-log, .bp-msg, .bp-composer, .bp-hero) that has no corresponding markup — leftover from a removed feature (~30 lines of dead CSS). Ironically, .bp-hero .chip (line 335) styles clickable suggestion chips, but the actual bp hero (bpHeroHtml, line 837) renders its three example prompts as plain italic text — you must retype them. Making the examples click-to-fill (composer prefill) is a one-liner and directly helps the Buildprint learning curve.
- **Cmd+K palette is files+chats only, and silently limited by the project filter** _[opportunity · ux:index.html]_
  palFilter (lines 1421-1428) matches over state.allFiles and chat titles only — no actions (new chat, new Buildprint task, digest, ingest, map/kanban/ideas, toggle filter), no full-text chat search (that exists but only via the sidebar input, line 531), and no fuzzy matching. Additionally state.allFiles is built from the FILTERED tree (line 1265), so with Project view on, Cmd+K cannot find manuals or crs-brain files and gives no hint why. For a keyboard-driven daily tool, the palette is the natural command hub; right now it's a partial file finder.
- **Escape doesn't reliably close the palette; explorer items break on quote characters** _[bug-risk · ux:index.html]_
  Global Escape (line 1626) only closes the explorer; the palette closes on Escape only while its input has focus (line 1442) — click a result row without navigating (or click inside the results area) and Escape does nothing. Separately, explorer folder rows strip apostrophes from paths instead of escaping them (line 1339: d.path.replace(/'/g,"") and the crumb at line 1330) — a directory named with an apostrophe would navigate to a nonexistent path and render 'empty'; file rows escape correctly (line 1343).
- **Minor token/copy drift: hardcoded colors and inconsistent Ingest naming** _[inconsistency · ux:index.html]_
  Non-token colors: .center-head background rgba(19,19,24,.55) (line 124) matches no surface token; mark.hl uses orange #F97316 (line 387) as a highlight accent, off the locked flat-#3B82F6 accent scheme (dashboard stat dots, lines 1015-1019, also hardcode a purple/violet family — arguably data-viz, but worth a token). Copy: the chat hero (line 836) tells you to hit 'Ingest to Brain', the actual button says 'Ingest' (line 610), the dashboard action says 'Ingest a report' (line 1027) — three names for one feature. Also FILE_RE (line 677) requires a slash, so root-level mentions of decisions.md — …
- **Mobile: Enter always sends, so multi-line prompts are impossible on phones** _[ux-friction · ux:index.html]_
  The composer keydown (line 1407) sends on Enter unless Shift is held; mobile virtual keyboards have no Shift+Enter, so on a phone every Enter fires the message. Standard fix: on coarse pointers / narrow viewports, let Enter insert a newline and rely on the send button. Also the placeholder still advertises 'Enter to send · Shift+Enter for newline' (line 592) on mobile where half of that is unusable.
- **Split-brain file opening between galaxy and board in embed mode** _[inconsistency · ux:map.html]_
  A galaxy node click hands off to the parent app viewer via window.parent.smartOpen (line 947), but a board card click always opens the in-map preview panel (line 1104) — the same file gets two different viewers with different capabilities depending on which surface you clicked. Additionally, the window.parent.openExpl / smartOpen accesses at lines 942 and 947 are not wrapped in try/catch (unlike line 1030), so a cross-origin embed would throw uncaught on every click.
- **Preview panel is read-only with no planning actions** _[missing-feature · ux:map.html]_
  The panel (openPreview, lines 1221-1281) has good md section tabs and per-section search/highlight, but offers no bridge into planning: no 'create idea from this file/section', no 'ask the brain about this section' (the dock is right below it), no edit/annotate. For .md planning docs this is where ideas are born, and the panel drops them on the floor.
- **List board layout is unreachable when the map is opened standalone** _[ux-friction · ux:map.html]_
  setBoardLayout (line 1335) is only invoked from applyView (lines 1340-1352), which is driven by the host app or the URL hash (#list) at boot. The board header (lines 236-244) has FILES/IDEAS tabs and text/tiles chips but no columns-vs-list toggle, so a standalone user can never reach list mode without knowing the hash.
- **Dead legacy Buildprint copilot endpoints and stale global session file** _[opportunity · server:capabilities]_
  GET /api/bp/history (server.js:979), POST /api/bp/reset (983), and POST /api/bp/chat (987-1029), plus BP_CHAT_FILE/loadBpChat/saveBpChat (lines 293-299), are called by neither index.html nor map.html — BP mode migrated to /api/chat with bp:true (index.html:1204). ~60 lines of dead code, a second divergent copy of the BP streaming logic to keep in sync, and a stale data/buildprint/chat.json with an old sessionId. Remove them or delete the file on startup.
- **BP task chats resume under BP_PROMPT even when created by the tracker, and branch drift breaks resume silently** _[bug-risk · server:capabilities]_
  Chats created by /api/bp/track and auto-track are saved with bp:true and a sessionId from a BP_TRACK_PROMPT run (server.js:958, 972, 1215, 1219); reopening and replying to one resumes that session via the /api/chat bp-branch (line 899) with BP_PROMPT instead — a silent system-prompt swap mid-conversation. Separately, resume assumes the workspace dir is stable: findBpWorkspace is re-resolved per message (line 900), so if the branch dir changes (test → dev), --resume with the old session in a new cwd fails with an opaque CLI error. Low frequency but confusing when hit.
- **LAN PIN gate: plain cookie, non-constant-time compare, PIN in URL query** _[bug-risk · server:capabilities]_
  pinOk (server.js:194-198) does a plain string compare of a cookie against the PIN, and login passes the PIN as a GET query param (?pin=..., lines 742-744) which lands in any intermediary logs; cookie is not HttpOnly. Acceptable for a personal same-Wi-Fi tool, but worth HttpOnly + POST for the PIN form if mobile mode gets regular use.
- **Buildprint MCP (open decision) would let build-packet verification steps be executed, not just written** _[opportunity · planning:knowledge]_
  PROGRESS.md line 41: CLI editing is wired, MCP (live data/logs/WU/tests) is offered but not set up, and --allowedTools already whitelists mcp__buildprint. Each build-packet step ends in a verification check (finding 5, section 8); with MCP those checks (does the DT exist, does the privacy rule evaluate, WU cost of a query) become executable from the same bp chat instead of manual Bubble-editor eyeballing. Worth setting up when Task 30 lands, not before.
- **Friction: shell-quoting gotcha in generated workflow commands** _[bug-risk · buildprint:manual]_
  cli-creating-and-copying-entities.md:84: `--param` specs containing `[]` or `?` (e.g. "users:user[]?") must be quoted in zsh or they glob-fail. Vlad's shell is zsh. Any generator emitting `buildprint new workflow --param` commands must auto-quote these specs; note also `new action --after/--before` inserts are rejected when positional PreviousStep references would break (lines 105, 196) — generator should default to appending actions rather than inserting mid-workflow.
- **Chat list has no topic separation between CRS planning and tool development** _[ux-friction · ux:live-walkthrough]_
  GET /api/chats returns 43 chats whose titles are raw truncated first messages with typos and near-duplicates ('the map backgeound shall expand below the chat input' x3). Only a boolean bp flag (3 chats) distinguishes anything; there is no tag/topic field, so genuinely valuable planning chats (the Pattern A migration plan, the 7-sections overview) are interleaved with CRS-Brain-UI debugging chats and 220-message Claude Code imports. Finding prior planning work means scanning titles.
- **PUT /api/ideas and PUT /api/progress accept any body unvalidated** _[bug-risk · ux:live-walkthrough]_
  PUT /api/ideas (server.js lines 1073-1078) writes the raw request body straight to the JSON file with no schema check; /api/progress PUT (line 1153) is the same pattern. A malformed client save (e.g. missing 'columns') silently replaces the board and the next GET returns broken shape to the UI. Recoverable via git history (autoCommit), but the API offers no guard.
- **decisions.md, specs/ and audits/ are invisible to the API; bp/history is a chat log, not an applied-changes ledger** _[opportunity · ux:live-walkthrough]_
  The repo contains decisions.md, specs/ (3 files) and audits/ (property-field audit) — prime planning inputs — but no structured endpoint surfaces them (only generic /api/file?path=). GET /api/bp/history returns just the copilot chat transcript, not a record of which Bubble changes were applied when (savepoints, apply events). Exposing a decisions/specs index plus an applied-changes ledger would give the dashboard the audit trail a plan-before-apply workflow implies. Positive note: /api/usage is the one genuinely rich endpoint (rate limits, context window, cost) and its 7-day limit at 64% is it…
