Saved a formatted, dark-themed spec doc at `crs-brain/data/docs/user-management-spec.html` (uses `design/tokens.css` colors). It covers all 5 sections — Data Model, Workflows, Privacy Rules, Permissions Catalog, UI — plus the two gap callouts at the bottom (empty Dependencies tab / all components still "Planned"; and the spec-vs-actual-build gap: security Pass-2 not done, User DT still property-only live).